# 实车直行优先探索

实车自动导航在完成原有扫描、墙面确认及定位检查后，先检查沿当前车头方向的直行动作。两侧更大的开口不再抢占一个可安全执行的直行步骤。界面显示“直行探索”和本步距离。

直行距离不超过 20 cm，并受 W 方向已配置的最小/最大步长限制。定位匹配分数低于 0.75 时沿用约 5.3 cm 的短步限制。若较长步骤不安全，按地图分辨率缩短到不低于底盘最小步长及默认 5 cm 探索步长，再检查；底盘最大步长小于 5 cm 时按其上限执行。每个候选都通过原有车体扫过区域、障碍膨胀、最新回波净空和路线进度检查。停车后按原流程重新扫描。

直行不能执行时，依次尝试排序后的可达探索目标；首个目标的动作失败不会直接终止全部目标搜索。之后仍保留开口探测与原有停车流程。

开放方向沿用“有效扫描中最多补充 1 m 假定空闲空间”的规则。整圈无效、定位结构不足、车体扫过区域未知时仍不允许自动直行。因此本次支持具备定位条件的开阔直道；雷达范围内完全没有足够墙面时仍会等待定位，不保证能够穿越。未修改雷达角度、标定、底盘参数或串口设置。

验证：`python -m unittest discover -s tests -p test_forward_exploration.py -v`。覆盖开阔区域不同车头朝向、前方障碍转向侧面、缩短直行步长、底盘步长及定位置信度限制、未知区域、扫描缺失，以及墙面扫描经建图线程生成纯前进指令。离线验证，未执行实车动作。
