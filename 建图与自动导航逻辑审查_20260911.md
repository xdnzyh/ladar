# 建图与自动导航逻辑审查（2026-09-11）

结论：分层设计和“停车扫描—定位建图—短步移动—停车复测”的基本方案合理，但当前实物、模拟界面、离线评估并非等价实现。已复现运动预测被覆盖、扫描完成门槛不当、拒收扫描消耗运动标记等问题；当前不能据此宣称全流程可靠或仿真已验证实车。

审查基于工作区 HEAD `75a4842`，开始时无未提交变更。检查了入口、运行配置、采集组圈、墙面确认、定位与栅格、前沿与路径规划、平移动作检查、底盘反馈、安全停车及终点判定。没有连接设备、发送串口命令或修改业务代码。新增文件仅为本报告与 `tmp/audit_*` 离线审查材料。

## 1. 三条实际执行链

- **实物界面**：双端时间同步/像素标定 → `TimedSweepBuilder` 真实相邻零位闭合 → `MappingRuntime` → 直线墙面拟合/连续两圈确认 → 扫描匹配/栅格累计 → 直行优先、前沿/A*、转弯探测、停车搜索 → 平移能力/已知自由空间/雷达净空检查 → 单次 MOVE → 编码器完成报告先验 → 停稳 → 新扫描准入。
- **模拟界面**：虚拟测距/旋转/通信 → `EstimatedSweepBuilder` → 同一个 `MappingRuntime` → 同一个导航核心，但无实物直行优先与补空闲策略 → 虚拟底盘、命令位移预测 → 按时间完成 → 新扫描。没有模拟串口 MOVE/RESULT 的完整事务状态机。
- **离线评估**：虚拟硬件/组圈 → 直接 `NavigationEngine.process_scan()` → 命令预测。跳过 `MappingRuntime`，不执行界面的墙面拟合、两圈确认和只累计障碍策略。

README 关于“EstimatedSweepBuilder 不参与正式组圈”和“旧 arbitrary_phase_scans 不切换来源”的描述已不符合代码：`virtual_hardware.py:259` 设置 `arbitrary_phase_scans=True`，`scan_acquisition.py:470` 据此选择估计组圈。停止后估计相位采样本身可以是模拟策略，但不能当作已测试实物相邻真实零位路径。

## 2. 优先处理的问题

### P1-1 模拟开始移动时未使后台建图失效，运动预测会被覆盖【已复现】

位置：`navigation_app.py:2316`–模拟动作分支；`mapping_runtime.py:249`–提交检查及覆盖。

实物 `_begin_hardware_motion()` 会增加建图代次并清理任务；模拟分支只设置 moving、执行动作并 `predict_motion()`，不改变 MappingRuntime 的代次/状态版本。已在后台运行的旧扫描因此仍可提交整份 navigator 副本。

确定性复现实际建图计算与预测交错：预测前 y=0.0100 m，移动预测后 y=0.10999 m，旧扫描提交后 y 又变成 0.0100 m，运动待处理标记也变成 false。普通互斥锁不能解决：计算在锁外，预测写入没有改变提交版本。

影响：模拟地图/位姿跳回、下一步基于错误先验规划；离线同步评估不会暴露此并发问题。

建议：两种动作入口统一使旧计算和待处理结果失效，运动先验更新必须推进状态版本；提交时检验该版本。

证据：`tmp/audit_mapping_race.py`、`tmp/audit_mapping_race.json`。该脚本执行真实 process_scan，通过事件固定提交时序，并断言丢失预测已复现。

### P1-2 新扫描准入误把“地图数值改变”作为必需条件【条件已复现】

位置：`navigation_app.py:1630`，`navigation_core.py:214`。

实物 WAITING_SCAN 要求扫描时间属于当前停车后窗口，同时要求 `map_version > 动作后基准版本` 和 completed_scans 增加。但栅格 log-odds 饱和到 ±20 后，合法、重复的扫描可以通过处理而不增加 `_revision`。

复现：10 次合法栅格更新，update_count 增加 10，revision 增加 0；将“停车后有效扫描、completed_scans 增加、地图版本不变”的结果交给真实 `_handle_mapping_results()`，`mark_scan_ready()` 未被调用。

影响：短步移动后若观测没有改变已经饱和的栅格，控制器可能一直等待，后续扫描也未必能解除。这里验证的是可触发条件，未声称已在实车上出现。

建议：准入使用“当前代次、当前会话、停稳后扫描、定位成功/扫描接受编号”，不要依赖地图内容一定发生变化。

### P1-3 实物地图范围不足以覆盖模拟场地【配置与边界已核验】

位置：`navigation_config.json`、`runtime_config.py:812` 附近的引擎构造、`navigation_core.py:157`–`207`。

实物地图 120×120、2 cm/格，总计 2.4×2.4 m；起点居中，前向最外格中心约 1.2 m。没有滚动地图或自动扩图，越界被视为占据；考虑车体足迹，实际可走中心区域更小。

当前模拟地图终点相对起点前向约 4.5 m，模拟栅格前向边缘为 5.6 m。实物这份配置不可能复现同样的完整路线。若只做原地局部雷达显示，该尺寸可以合理；自动导航不能直接沿用局部显示范围。

建议：把显示视野和导航地图尺寸分开，按场地扩大地图或实现滚动/分块地图，并明确显示“地图边界导致无路”。

### P1-4 评估入口跳过实际建图流程，当前成功指标也不验证泊车【代码确认】

位置：`evaluate_simulation.py:100`、`:113`；对照 `mapping_runtime.py:208`–`243`。

离线评估直接处理原始点：没有两圈确认、没有墙段拟合、没有界面 `add_only=True`，还没有异步提交竞争。出现近障安全停车时，评估也没有调用模拟界面使用的 `request_obstacle_recovery()`，控制恢复行为不同。

`reached_goal` 只判断真值距参考终点 ≤0.20 m，即使尚未“泊车完成”也会记为成功。它适合衡量接近参考点，不足以衡量完整自主泊车；反过来，算法认定的死端也未必就是地图编辑器参考点。

建议：评估共用实际处理与状态转换，分别记录接近目标、正确泊车、错误泊车、超时、安全停止；增加带完整 MOVE/RESULT 事务的硬件适配层仿真。

### P2-1 被拒收的扫描提前清掉运动标记，终点确认可能无法启动【已复现】

位置：`navigation_core.py:1097`–`1104`，`:1453`。

process_scan 在检查点数、匹配分数之前清掉 `_translation_since_last_scan` 等标记。若动作后第一圈被拒绝，随后合法的静止复扫会被看作“此前没有平移”；而终点连续确认从 0 开始时恰好要求刚平移过。

复现：预测移动后该标记为 true，空扫描处理后变为 false，随后终点条件不能启动。实际调用也可能由点数不足或定位失败触发，不仅是空扫描。

影响：已到端部、又没有安全微移动作时，可能持续复扫而不能结束。运动标记还用于控制未定位前是否能写自由空间，提前消费也扩大了错误先验被使用的机会。

建议：运动上下文应保留到定位成功，或为终点确认使用独立的“自上次成功定位以来发生移动”状态。

### P2-2 终点几何判据弱于“唯一开口是来路”【判据已复现】

位置：`navigation_core.py:1452`–`1480`。

当前前/右/左仅需两侧 <0.30 m，加后方 ≥0.90 m 就通过几何检查。输入前=.25、右=.25、左=1.0、后=1.0 m，检查返回 true。第三方向可以仍是明显开口；后方也固定为车体后方，没有按实际来路方向判断，麦克纳姆横移绕弯后两者可能不同。

完整规划会先尝试前沿、探测、停车微移，所以该复现证明的是几何判据过宽，不等于每次有侧开口都会错误泊车。但当开口因未知格、最小步长或路线限制暂不可执行时，确实存在把“无可执行动作”误解成“到达终点”的条件。

建议：区分封闭、未知、开放但不可执行；判定所有非来路出口均关闭，按轨迹确定来路。测试要覆盖左/右侧开口和横移后的死端。

### P2-3 只累计障碍使错误墙无法被后续观测消除【已复现，策略取舍】

位置：`mapping_runtime.py:243`，`navigation_core.py:338`、`:355`。

界面导航固定使用 add_only=True。自由射线遇已有障碍就停止，正证据也不会被自由证据降低。复现：0.4 m 处已有 log-odds=6 的墙格，后续 50 次明确无回波扫描后仍为 6、仍是障碍。

对于固定场地防止墙面被擦除，这有明确用途；但两圈一致的定位偏差、重复杂点或已移走物体一旦写入，就可能形成永久假墙，抹不掉的假墙会破坏前沿与单向进度连通性。离线评估的默认可清除栅格不会暴露同样的问题。

建议：保留短期原始障碍层和长期结构层；允许高置信、跨多圈、定位稳定的反证清除，或提供明确局部修正机制。

## 3. 需要明确的策略和文档差异

这些行为有明确代码/文档依据，不应未经确认就作为“修复”删除，但必须纳入仿真条件与实车验收。

| 项目 | 当前实物 | 当前模拟界面 | 判断 |
|---|---|---|---|
| 可信量程 | 0.15–1.00 m | 0.08–3.00 m | 观测能力差异很大 |
| 地图 | 2.4×2.4 m，2 cm/格 | 7.2×11.2 m，4 cm/格 | 路线和膨胀离散性不同 |
| 无观测方向 | 满足点数条件后补最多1 m空闲射线 | 仅显式无回波自由射线 | 实物含可通行假设 |
| 前进偏好 | 直行优先开启 | 关闭 | 共用引擎不等于共用探索策略 |
| 停车后组圈 | 真实相邻零位 | 根据稳定转速估计采样窗口 | 延迟/质量拒收不同 |
| 位移先验 | 编码器 ENC/系数，按命令方向 | 命令速度×持续时间 | 两者均需匹配纠偏 |
| 新扫描门槛 | WAITING_SCAN 特殊准入 | 无底盘扫描准入事务 | 实物卡等待不一定能在模拟出现 |
| 安全停止 | 停止自动运行 | 指定近障情形可进入恢复 | 恢复表现不能直接横比 |

**1 m 补空闲是已记录的用户策略**，见《导航空闲空间说明.md》，不是本次发现的意外代码。原始回波会对补充射线形成遮挡，空扫描不补齐，这些限制合理。但代码把假设空间作为普通 FREE 交给规划，后续“只走已知自由空间”不能独立证明那里真实无障碍。应将假设来源可视化，并在模拟加入漏测/低反射场景检验，而非与3 m显式射线混为一谈。

**距离控制分支已绕过部分实测准入**：当前配置 `chassis_distance_control=true`。`chassis_controller.py:142` 起跳过速度/停止距离/方向验证与不确定度检查；`runtime_config.py:796` 重新开放八个方向。复现当前配置的 adapter.readiness()=(true, 无原因)，并可生成自动 W717 CNT（约0.1 m）请求，即使速度/制动验证为 false、各方向 motion_range_validated=false。完整实物发送仍有连接、固件、参数同步和底盘状态门槛，不能说启动即会移动。

编码器距离控制可以控制计数目标，但不等于已证明地面位移、滑移或停止距离。当前缺失的执行不确定度用一格分辨率代替，平移 yaw 不确定度为0（`chassis_controller.py:354`）。建议把“支持距离目标”与“运动误差经过验证”分开，明确哪些测量证据仍必需，并同步 README 的默认锁止描述。

## 4. 已执行验证

### 全套回归

运行 `python -m unittest discover -s tests`（实际使用桌面自带 Python 绝对路径）。416 项，404通过、3失败、9跳过，耗时51.07秒。9项跳过是代码中明确跳过的历史 MEASUREMENT_SYNC_V2 回归。

失败项及解释：

1. `test_manifest_matches_user_supplied_files`：旋转基准固件实际 SHA256 与 manifest 不符，基准来源证据需要整理；不能据此断言设备烧录了错误固件。
2. `test_actual_front_and_left_walls_survive_and_isolated_echo_does_not`：采集层保留了孤立点；同文件后续墙面准备过滤测试通过，因此不能直接说孤立点已被写成地图障碍。
3. `test_only_noise_or_no_return_still_times_out`：期望12.1秒停止，实际仍运行。当前 `PIX -1` 已被定义为有效 over_range，自由空间策略与旧测试预期存在冲突，需统一有效观测/故障定义。

日志：`tmp/audit_tests.txt`。仓库既有《导航空闲空间说明.md》也记录了同名三项失败；本次没有额外检出历史版本重跑，因此不主张已完成回归起因定位。

### 原有离线评估：每档1个种子、120秒

地图为当前 simulation_map.json，种子20260907；所有结果都是合成仿真。

| 档位 | 120秒内到参考点 | 剩余距离 | 位置RMSE | 偏航RMSE | 碰撞接触 | 安全停止 |
|---|---|---:|---:|---:|---:|---:|
| IDEAL | 否 | 2.636 m | 0.00171 m | 0° | 0 | 0 |
| NOMINAL | 否 | 3.215 m | 0.01020 m | 1.27° | 0 | 1 |
| STRESS | 否 | 3.152 m | 0.09908 m | 11.03° | 0 | 1 |

未到达只说明本次限时运行没有完成，不证明永久不可达。零碰撞也不能替代实车安全证明；评估路径差异见 P1-4。单种子不支持可靠成功率统计。

数据：`tmp/audit_simulation.json`。

### 实际 MappingRuntime 处理链对照：各60秒

另运行 `tmp/audit_runtime_comparison.py`，接入真正的墙面拟合、两圈确认、栅格策略与规划；共同采用IDEAL虚拟物理，实物配置使用1 m量程，模拟使用3 m。实物分支在动作前使墙面证据失效，仿照界面停走流程。

| 配置 | 动作数 | 结束状态 | 剩余距离 | 碰撞接触 |
|---|---:|---|---:|---:|
| 模拟策略 | 17 | 探索中 | 3.361 m | 0 |
| 实物策略 | 11 | 两圈墙面确认 | 3.851 m | 0 |

这验证两条配置在实际建图层能形成运动闭环，但没有验证全程完成。此对照使用理想命令先验，未模拟真实像素标定、双端时钟估计、串口事务、完整GUI调度或 WAITING_SCAN；不能称为实物端到端验收，也不能由动作数量推出谁更优。运行终止时“等待第二圈”是采样时刻状态，不单独判定为卡死。

数据与状态历史：`tmp/audit_runtime_comparison.json`。

### 确定性针对性证据

- `tmp/audit_navigation_probes.py` / `.json`：配置差异、距离控制准入、第三侧开放时终点判据、拒收扫描消耗标记、假墙不可清除、栅格饱和及实际GUI扫描准入条件。
- `tmp/audit_mapping_race.py` / `.json`：真实建图过程与模拟动作预测并发导致位移丢失。
- 全部不使用串口、不产生实体动作。复现脚本用于本次审查，尚未改写为修复后的回归测试。

## 5. 合理的部分与处理顺序

合理的设计包括：协议观测与导航解耦；实物会话/时间/完整扫描约束；动作期间暂停正式建图；低置信度停车复扫；车体扫过区域、未知格、膨胀障碍及原始近障多重检查；平移上限与禁旋转；单次 MOVE、不因 ACK 丢失重发；DONE 后先验与停稳/重扫分离。这些都有对应实现与回归覆盖。

适用任务是预设单向通道探索，不是任意迷宫导航。禁止回溯的路线进度约束可以防止走回头路，但对需要退让、回退或绕障的环境会降低完备性。当前也没有通用全局重定位/回环优化，定位退化后主要依靠停住重扫。

建议处理顺序：先修提交版本/运动预测一致性和新扫描准入，再修拒收后的状态保留；随后统一评估入口并扩展实物导航地图；最后收紧终点几何、评估长期障碍纠错，并把补空闲和距离控制的适用条件写入统一配置与验收标准。每一步应以对应复现作为回归，不应先通过降低门槛让车“动起来”。
