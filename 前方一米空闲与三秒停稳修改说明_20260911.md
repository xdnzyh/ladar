# 前方一米空闲与三秒停稳

## 已实现

1. 实物模式已有的一米无回波补全，现在会将前方半平面内、没有障碍遮挡的栅格一次更新为 FREE（已知空闲），不再等待负权重多圈累计。补齐射线栅格化产生的零星未知格。传感器安装偏航参与前方方向计算。仍保留“假设空闲”的来源标记，供后续探索判断使用。
2. 已收到匹配动作结果后，机械停稳等待采用配置值与 3 秒的较小值，当前默认仍为 0.2 秒。增加轮询超时补偿：正常定时回调未执行时，3 秒期限到达后结束同一动作的 SETTLING 阶段，恢复后续扫描流程。

## 明确边界

- 空扫描、无有效数据不等同于没有墙。已有障碍格、当前回波所形成的遮挡、1 米之外不强制清空。
- 3 秒规则不是“运动结果缺失也强行解锁”。等待 ACK/DONE、停止尚未确认、手动急停或故障不会被该计时器绕过。
- 结束机械停稳后进入 WAITING_SCAN；下一条运动仍需停稳后的新扫描。这项修改不等同于“3 秒后一定发出 MOVE”。
- 定时器和补偿检查都依赖应用事件循环运行；整个进程或界面线程挂死时，不保证墙钟精确 3 秒完成释放。

## 验证

新增 `tests/test_front_free_settle_limit.py`，覆盖前方整片栅格一次置空闲、一米边界、障碍遮挡、空扫描、安装偏航、停稳时限、遗漏回调补偿、停止未确认和旧动作计时器隔离。

完整回归输出：`tmp/front_free_settle_tests.txt`。本次没有启动实车；重启电脑端导航程序加载修改。
