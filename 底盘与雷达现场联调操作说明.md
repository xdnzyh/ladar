# 底盘与雷达现场联调操作说明

适用入口：`start_hardware_navigation.bat`。测距、旋转和底盘使用三路独立串口；测距与旋转保持两个独立 ESP32，底盘为 Arduino UNO/AVR 控制器。

初始配置不会开放自动运动：底盘能力为 `unknown`、固件未确认、各方向运动范围与不确定度为空。现场参数保存后需断开并重新连接设备才能生效；未完成范围验证的方向继续锁定自动导航。

## 1. 核对固件、接线和端口

1. 车辆置于不会因误动作造成碰撞的位置，操作人能够立即按下“紧急停止”。
2. 启动程序，点击“刷新串口”，分别选择测距、旋转、底盘端口。三个端口不得重复；雷达为 115200，底盘为 9600、8N1、无流控。
3. 打开底盘原始接收日志，核对完整启动标记：

```text
MECANUM UNIVERSAL V6.3 COMM READY
RX_FRAME_GUARD=1 SERIAL=9600,8N1
BRAKE_CAL=4 ALL8 SHORT-LONG
DIST_CAL=1 MM=WSADQEZC
COMM_GUARD=3 LOG=0 PING=1
```

4. 五行均确认后，在现场参数面板选择能力模式 `mm_ping_v1`，勾选“固件已确认”；只有旧 CNT 协议证据时选择 `cnt_only`。仅看到 V6.3 主横幅不足以确认 MM/PING 能力，不为获取横幅自动复位车辆。

| 判定 | 内容 |
| --- | --- |
| 通过 | 三路端口唯一，波特率正确，能力标记与所选模式一致，车辆保持静止。 |
| 失败 | 端口重复、启动标记缺失或冲突、串口持续重启、未知文字被误识别为能力证据。 |
| 停止条件 | 出现非预期运动、重复启动横幅、供电或无线模块状态异常。立即点击“紧急停止”，不要继续发命令。 |
| 保留证据 | 端口映射、五行启动原文、主机时间戳、底盘固件文件版本及最终载荷照片。 |

## 2. 静止状态检查与 20 次通信检查

1. 点击“连接设备”，确认三路连接状态分别显示；仅检查雷达时可不连接底盘。
2. 车辆保持静止，点击“读取底盘状态”。有效空闲回复应以 `IDLE X=... Y=... R=... S=...` 开头；这些数值是编码器组合量，不是米或弧度。
3. 点击“通信检查（20次）”。检查只执行带随机 nonce 的 PING/PONG，不发送 MOVE。
4. 保存首发成功次数、重试成功次数、各次延迟、错误 nonce、BUSY、截断和启动横幅重现。`logs/chassis_serial.jsonl` 记录 RX/TX 原始字节及对应主机时间；现场检查期间保持“底盘原始日志”启用。

| 判定 | 内容 |
| --- | --- |
| 通过 | 20 次均在首发收到完全匹配的 PONG，无重启、截断、BUSY或错误 nonce。首发与重试统计均完整。 |
| 失败 | 任一次只能靠重试成功、PONG 不匹配、持续接收导致静默窗口超时、回复截断或设备重启。通信可诊断，但不进入运动阶段。 |
| 停止条件 | 通信检查期间出现 MOVE/ACK/DONE、车辆运动或启动横幅重现。 |
| 保留证据 | CHECK20 汇总、事件日志、`logs/chassis_serial.jsonl` 中的 RX/TX 原始字节与主机时间、开始与结束时间、无线模块及跳线状态。 |

## 3. 原地验证雷达采集与正式组圈

1. 不发送底盘动作，分别确认测距数据和旋转 TRIG 数据持续到达。
2. 确认正式扫描由相邻两个真实 TRIG 闭合；预览点、残缺圈和仿真估计相位不计入正式地图。
3. 记录 100 Hz 请求下的实际有效接收率、完整圈数量、时间同步质量、过期观测、校时拒绝和距离标定拒绝。
4. 观察静止地图是否稳定，核对雷达零角是否朝车头正前方；安装偏移留到第 6 阶段保存。

| 判定 | 内容 |
| --- | --- |
| 通过 | 连续产生满足当前质量门限的真实完整圈，校时有效，静止环境地图不持续漂移。 |
| 失败 | 只有预览点、无相邻 TRIG 闭合、持续同步故障、有效接收率不足或地图明显漂移。配置为 100 Hz 不等于实际达到 100 Hz。 |
| 停止条件 | 激光、旋转机构或串口异常；停止采集并保留当次数据。 |
| 保留证据 | 测距/TRIG 原始日志、完整圈统计、实际接收率、校时质量、静止地图截图及配置快照。 |

## 4. 现场授权一次短平移

1. 清空运动方向上的人员和障碍，操作人保持在“紧急停止”旁。
2. 选择一个平移方向，`mm_ping_v1` 优先选择单位“MM”。首次可用 100 mm 核对协议：例如 D 方向应发送 `@MOVE,D,100,MM`，预期 `TARGET_CNT=739`。CNT 模式只发送已经核对过的整数 CNT。
3. 勾选“现场单步授权”，点击“发送人工动作”。每次只发送一条 MOVE；缺 ACK 或 DONE 时绝不重发。
4. 核对 ACK 的模式、原始请求值和单位；核对 DONE 的终态、REQ、UNIT、TARGET_CNT、ENC 和 Q1～Q4，并实测运动方向与停车后位移。

100 mm 的固件换算用例：W718、S713、A724、D739、Q1018、E912、Z965、C1019。它们只用于核对换算，不代表物理位移已经验收。

| 判定 | 内容 |
| --- | --- |
| 通过 | 单次请求与 ACK/DONE 完整匹配，终态为 TARGET，方向正确，车辆停稳，实测位移已记录。缺 ACK 但完整匹配的有效 DONE 可作为动作结束证据，同时记录缺 ACK。 |
| 失败 | BAD_*、BUSY、TIMEOUT、WRONG_DIRECTION、EMERGENCY、字段缺失/重复、TARGET_CNT 不符、方向错误、报告截断或启动横幅重现。 |
| 停止条件 | 车辆朝错误方向、接近边界、通信中断、报告归属不明或制动异常。点击“紧急停止”，不得用下一条 MOVE 试探。 |
| 保留证据 | 原始 TX/RX、ACK/DONE 全文、主机时间戳、实测方向和位移、停止方式、现场视频或照片。 |

## 5. 补测自动导航所需范围

1. 只测试计划实际使用的平移方向和距离，不重复无关的整套标定，不测试未标定的 R/F 自动旋转。
2. 每个方向分别取得可重复的最小/最大请求距离、执行不确定度、实际速度上界和完整停止距离；同时记录电池、地面、载荷和轮胎状态。
3. 八方向已有 CNT/mm 初值不得标成“未标定”，也不得直接标成“已验证”。A 长档前漂、D/E 短长比例差等串扰保留在证据中。
4. 在“保存底盘现场参数”中录入每方向验证范围和不确定度，并分别确认速度上界与制动距离。没有数据的字段保持为空，不填默认值解锁。

| 判定 | 内容 |
| --- | --- |
| 通过 | 所需方向均有可追溯重复数据，最小值不大于最大值，不确定度、速度上界和完整停止距离均来自现场测量。 |
| 失败 | 仅有单次样本、测量条件变化未记录、横漂/偏航明显、短距离不可重复或只有编码器停车误差而无物理停止距离。 |
| 停止条件 | 任一方向失控、停止距离越界、通信结果不明或现场空间不足。该方向保持自动锁定。 |
| 保留证据 | 每次请求、TARGET_CNT、ENC、实际位移、用时、完整停止距离、重复统计及环境条件。 |

## 6. 保存最终几何与安装参数

1. 使用最终电池、载荷和机械安装，测量底盘外形/安全包络、雷达相对车体中心的平移偏置和零角。
2. 在现场参数面板录入底盘尺寸、雷达偏置、速度上界、制动距离以及第 5 阶段通过的方向范围。
3. 点击“保存底盘现场参数”，随后断开并重新连接设备。
4. 重新打开现场参数面板，逐项核对保存值；未验证方向应继续显示为锁定。

| 判定 | 内容 |
| --- | --- |
| 通过 | 重连后参数与测量记录一致，数值有限、单位正确，自动预检只开放证据完整的方向。 |
| 失败 | 保存后数值变化、米/毫米或角度/弧度混用、未知方向被启用、R/F 在无旋转标定时开放。 |
| 停止条件 | 机械安装、载荷、轮胎或雷达位置改变。相关现场参数立即失效，停止自动导航并重新核对。 |
| 保留证据 | 测量表、配置文件副本、保存时间、重连后的参数页截图和安装照片。 |

## 7. 验证单段扫描—移动—校正闭环

1. 在开阔区域取得一圈有效停车扫描。
2. 允许一段已验证范围内的短平移，观察顺序：规划 → PING/PONG → 单次 MOVE → ACK/DONE → 停稳等待 → 新完整扫描 → 雷达校正。
3. 确认位姿只在有效 DONE 后应用一次编码器先验；移动、制动及停稳前的扫描不得写入正式地图。
4. 只有新的真实完整扫描及定位质量检查通过后，才允许下一段动作。

| 判定 | 内容 |
| --- | --- |
| 通过 | 一条 MOVE、一次执行先验、一个动作后的新完整圈；地图和位姿在新扫描后完成校正，无重复累计。 |
| 失败 | 写出 MOVE 时先累计目标位移、同一 DONE 重复积分、旧扫描解除互锁、移动扫描写图、缺失 DONE 后继续规划。 |
| 停止条件 | 任何异常终态、启动横幅重现、无法归属的迟到 DONE、定位质量失败或停止状态未确认。 |
| 保留证据 | 完整事件时间线、TX/RX、动作前后扫描编号、位姿快照、地图质量和故障状态。 |

## 8. 分阶段开放导航与泊车

1. 预检全部通过后点击“开始导航”，依次验证开阔直行、45°平移、转角、近障、通信故障和泊车；每次只扩大一个场景。
2. 长路径由多段短动作组成，每段结束后重新扫描和规划；不一次排队多条 MOVE。
3. 近障、盲区、过期测距、未知区、膨胀障碍或通信状态不明时应停车，不能用更长动作绕过安全检查。
4. 泊车要求前/左/右至少两侧严格小于 0.30 m，并满足既有车位几何；连续三次有效确认后停止底盘和雷达，状态保持“泊车完成”。

| 判定 | 内容 |
| --- | --- |
| 通过 | 各场景分别留下完整闭环证据；故障注入能锁定后续动作；泊车满足几何条件并连续确认三次。 |
| 失败 | 不支持方向被规划、动作被发送端静默裁剪、未知区通行、普通停止/定位失败被当成泊车成功，或故障后自动续航。 |
| 停止条件 | 点击“紧急停止”，或出现近障、盲区、串口断开、超时、设备复位、地图/定位失效、运动结果不明。停止未确认前不得重置、清图或重连后继续原任务。 |
| 保留证据 | 每个场景的配置、原始通信、扫描与地图快照、安全停车记录、泊车三次确认记录及人工实测结果。 |

完成上述现场记录前，只能确认软件与离线验证状态；不得把“现场通信通过”或“实车建图与导航通过”标记为已完成。
