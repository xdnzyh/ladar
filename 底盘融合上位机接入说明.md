# 底盘融合上位机接入说明

## 当前状态

上位机已按当前 `car control/control.ino` 接入底盘 ASCII 行协议。底盘固件未修改、未重新编译、未烧录；本文件中的串口动作和装车步骤是待现场执行的方案，不是实车测量结果。

已完成的软件范围：

- 雷达两路继续使用 115200；底盘使用独立的 9600、8N1 配置。
- 支持 `@MOVE`、精确匹配的 `@ACK`、严格字段校验的 `@DONE` 和 `@ERR`。
- 支持分包、CR/LF/CRLF、调试文本交错、超长行、坏字节、重复 ACK/DONE 和连接代次隔离。
- 自动控制一次只保留一个在途动作；ACK 超时不重发 MOVE，DONE 超时、错误回复、传输异常和停止未确认均锁定自动控制。
- 停止写入 `!\r\nX\r\n`，同时取消尚未由串口写线程取出的普通写入；串口关闭、无线缓冲和底盘机械状态不能被软件假定为已清空。
- DONE 之后才应用一次经标定的执行先验；正式建图在动作发送边界、制动、停稳等待和新完整扫描之前保持关闭。
- 默认十个方向均为 `uncalibrated`，速度上界、停止距离和制动状态也未填入有效测量值，因此自动实车 MOVE 默认拒绝。人工短动作入口仍使用 CNT。

## 文件和职责

| 文件 | 作用 |
| --- | --- |
| `chassis_protocol.py` | 底盘字节分帧、严格协议解析、DONE 运动学一致性检查、规范 MOVE 和停止字节。 |
| `chassis_controller.py` | 连接代次、动作代次、ACK/DONE 生命周期、超时、停止、状态不明、CNT 换算和执行先验。 |
| `serial_backend.py` | 每次打开串口使用独立会话停止事件、读写线程和回调，避免旧串口线程污染重连后的会话。 |
| `navigation_app.py` | 三路连接、人工 CNT 入口、自动导航准入、扫描窗口、状态显示和 Tk 主线程事件处理。 |
| `runtime_config.py` / `navigation_config.json` | 雷达/底盘波特率、超时、动作上限、标定状态、速度/制动测量准入。 |
| `navigation_core.py` | 通过 `apply_execution_delta()` 接收一次性执行先验，保留原共享地图、定位、规划和单向约束。 |
| `motion_safety.py` | 使用经配置的底盘速度上界和停止距离；缺少实测上界时不放行自动动作。 |
| `tests/test_chassis_integration.py` | 协议、停止字节、状态机、超时、方向换算和未标定拒绝的离线测试。 |

## 底盘协议事实

以下内容来自当前 `control.ino` 源码；README 中的历史记录不替代源码和现场测量。

```text
固件标识：MECANUM UNIVERSAL V6.3 COMM READY
UART：9600
MOVE：@MOVE,<MODE>,<正整数>,CNT\r\n
ACK：@ACK,<MODE>,<正整数>,CNT
DONE：@DONE,<MODE>,<TARGET|EMERGENCY|TIMEOUT|WRONG_DIRECTION>,...
停止：!\r\nX\r\n
```

`ACK` 只表示命令已被接受；`DONE` 在制动、释放和最终编码器报告之后输出。`REQ` 是请求计数，`BRAKE` 是开始制动时的进度，`ENC` 是制动释放后的最终主动轮进度。`DX/DY/DR/DS` 与 `Q1~Q4` 是本次动作相对增量，不是上电累计里程。

底盘动作方向使用 `W/S/A/D/Q/E/Z/C/R/F`。底盘坐标为 `+X=前、+Y=左、+R=顺时针`；上位机坐标为 `+x=右、+y=前、yaw 正=顺时针`。因此已完成物理标定的报告统一在 `chassis_controller.py` 中转换为：

```text
上位机 Δx = -底盘左向位移
上位机 Δy = +底盘前向位移
上位机 Δyaw = +底盘顺时针转角
```

`ENC` 不是毫米或弧度。斜移的 `ENC` 是主动轮沿斜向的计数，不能直接当作 `DX` 或 `DY`；标定项必须说明 CNT 对应斜线长度还是轴向分量。

## 动作状态

| 状态 | 可发送 MOVE | 可恢复正式建图 | 进入条件 |
| --- | ---: | ---: | --- |
| `disconnected` | 否 | 否 | 串口未打开或已失效。 |
| `connected_waiting` | 仅人工 | 否 | 串口打开，尚未收到启动标识、IDLE、ACK 或有效错误回复。 |
| `idle` | 人工；自动需准入 | 仅静止扫描 | 协议状态已确认且没有在途动作。 |
| `waiting_ack` | 否 | 否 | 已登记动作并排队发送。 |
| `waiting_done` | 否 | 否 | 已收到匹配 ACK，等待完整 DONE。 |
| `stopping` / `stopping_idle` | 否 | 否 | 已写停止字节，等待动作 DONE 或有界的 `P` 状态确认。 |
| `settling` | 否 | 否 | 已收到匹配 DONE，等待板上制动之外的上位机停稳时间。 |
| `waiting_scan` | 否 | 否 | 自动动作已停稳，等待停稳后的新完整相邻 TRIG 圈。 |
| `unknown` | 否 | 否 | ACK/DONE 超时、字段冲突、断连、停止未确认或无法消除的动作归属歧义。 |

自动序列只允许以下闭环：

```text
idle → waiting_ack → waiting_done → settling → waiting_scan → idle
```

ACK 超时不重发原 MOVE。`DONE` 先于 ACK、相同 MODE/REQ 的旧报告无法消歧、停止超时或断连时保持 `unknown`，需要现场确认静止、重新连接并重新定位。

## 自动动作换算

规划器仍输出 `VelocityCommand` 作为意图，但当前固件不支持速度、持续时间或四轮 PWM 字段。上位机先将意图限定为一个离散方向：纯前后、纯左右、等幅 45° 斜移或纯旋转；平移与旋转混合、非等幅斜移直接拒绝。

```text
forward_m = forward_mps × duration_s
right_m   = right_mps × duration_s
yaw_rad   = yaw_rps × duration_s
```

再使用对应方向的实测 `counts_per_m` 或 `counts_per_rad` 生成正整数 CNT，并检查最小可重复动作、最大 CNT、0.20 m 初始平移上限和旋转上限。没有方向标定、不确定度、速度上界或停止距离时，人工入口仍可发送用户明确输入的 CNT，但自动入口拒绝。

收到有效 `TARGET` 后只使用一次 `ENC` 和该方向的标定系数生成执行先验；不先加计划位移，也不再把编码器结果加一次。`EMERGENCY`、`TIMEOUT`、`WRONG_DIRECTION` 只保存原始执行报告，自动序列停止并要求重新定位。

## 停止处理

当前固件在 `protocolReceiving` 状态下会把 `X` 当作协议行内容。上位机因此发送：

```python
b"!\r\nX\r\n"
```

其中 `!` 使未完成的 `@MOVE` 行变成无效命令，换行结束该行，最后的 `X` 才进入固件的急停分支。离线验证覆盖了规范 MOVE 的所有未完成截断位置、CR/LF 边界和超长行恢复；如果 MOVE 已经完整提交到底盘，任何后续停止序列都不能撤回已经接受的动作。

`STOP_BEGIN` 在固件实际 `brakeAll()` 之前输出并 flush，之后约 300 ms 制动、释放、读取编码器和输出完整报告。上位机不把 `STOP_BEGIN`、写完成回调、计划时长或 `STOP_RELEASED` 当作停稳证据；`hardware_settle_s` 只是报告收到后的额外等待，默认 0.2 s 仍需现场测量。

## 离线验证

在项目目录执行：

```powershell
python -m unittest tests.test_chassis_integration -q
python -m unittest discover -s tests -q
```

离线测试使用假传输和固定时钟，不打开真实 COM、不发送真实运动字节、不编译或烧录固件。覆盖内容包括：

- 十种协议 MODE、分包、合包、三种行尾、调试文本交错和完整 DONE。
- 缺字段、重复字段、未知字段、非有限数值、坏字节和过长行。
- DONE 的 `Q1~Q4` 运动学一致性、重复 DONE、DONE 早于 ACK、模式/REQ 不匹配。
- ACK 超时只发一次停止序列，不重发原 MOVE。
- 停止中保留原动作归属，异常报告不恢复自动动作。
- 十方向符号、斜移等幅要求、CNT 换算、底盘左向到上位机右向的单次符号转换。
- 未标定方向和缺少速度/制动测量时拒绝自动动作。
- 现有同步采集、建图、仿真、单向路径和运动安全回归。

这些结果只证明软件协议闭环和既有回归，不证明无线延迟、底盘真实位移、轮胎滑移、速度、制动距离、雷达外参或实车地图精度。

## 装车检测与标定

### A. 三路链路确认，不运动

1. 为三对无线模块保留三个独立信道；核对测距/旋转电脑端 UART 为 115200，底盘电脑端 UART 为 9600、8N1。
2. 在配置中填入实际 `measurement_port`、`rotation_port`、`chassis_port`。COM 号必须以现场设备管理器和串口列表为准，不由软件猜测。
3. 连接后先被动观察底盘启动标识；没有启动标识时点击“读取底盘状态”，只发送源码支持的 `P`。
4. 保留雷达两路和底盘一路独立原始日志，观察底盘通道是否混入 `PIX`、`TRIG` 或其他链路文本。
5. 通过条件：三路独立、无串音、底盘能返回启动标识或合法 `IDLE`；失败时断开、保留日志，不继续发送 MOVE。

### B. 架空轮和单动作

1. 架空确认 W/S、A/D、Q/E/Z/C、R/F 的编码器符号；架空结果不能代替地面位移和滑移测量。
2. 在人工面板一次只输入一个经现场负责人确认的短 CNT，发送后记录 `@ACK`、`START MODE`、`STOP_BEGIN`、`STOP_RELEASED`、完整 `@DONE` 和每条消息的 PC 单调时间。
3. 分别验证运行中停止、空闲停止、重复点击、停止后迟到 TARGET、断开和重连。停止未确认时界面必须保持“停止中/状态不明”，不能重新开始自动动作。
4. 通过条件：ACK/DONE 字段完整且匹配，重复报告不重复计数；失败时保留独立停机手段，禁止在落地小车上继续做协议模糊测试。

### C. 雷达坐标和外参

1. 已确认光电门触发瞬间光束朝车头正前方，因此零角定义为前向；不额外添加 90° 或 180° 补偿。
2. 用前、右、后、左静态目标检查 0°、90°、180°、270° 分别对应正前、正右、正后、正左。
3. 实测雷达有效测距原点相对车体参考点的右向/前向偏移、车体长宽和外伸部件；光电门位置不能替代测距原点。
4. 改变零角或外参后清除旧地图并重新建立静止参考，不沿用旧坐标自动运行。

### D. 十方向物理标定

W/S、A/D、Q/E/Z/C、R/F 分别重复测量，不能由一个方向的系数推导其他方向。每次记录固件标识、MODE、REQ、BRAKE、ENC、DX/DY/DR/DS、Q1~Q4、实测平移/转角、停止开始到最终静止时间、电源、载荷、地面、轮胎状态、无线配置和原始日志。

平移方向拟合 CNT 到物理位移的关系，旋转方向拟合 CNT 到物理角度的关系；另行记录制动超调、最小可重复动作、有效范围和异常样本。斜移必须说明 CNT 对应斜线长度还是轴向分量。外部尺量或角度基准负责物理标定，雷达只能交叉检查，不能与编码器构成循环自证。

### E. 雷达—底盘闭环

只开放已经通过标定的方向，逐段执行：

```text
静止完整扫描 → 一个短 MOVE → 匹配 ACK/DONE → 停稳等待 → 新完整扫描 → 比较执行先验与雷达校正
```

检查运动窗口没有正式地图更新，没有把计划和 ENC 相加两次；匹配失败、扫描覆盖不足、位置校正过大或安全观测失效时保持停车。

### F. 自动探索

只有通信、停止、速度/制动边界、十方向所需标定、雷达外参、定位质量和车体扫掠检查全部通过后，才在配置中逐步开放自动准入。终点仍须连续复测；单次 TARGET、仿真成功、ACK/DONE 或 UI 截图都不能替代整车验收。

## 当前无法由上位机消除的限制

- 当前协议没有动作 ID、会话号、设备时间戳、CRC、停止 ACK、通信断链看门狗和全局急停抢占。
- PC 连接代次只能隔离 PC 自己的旧线程和回调，不能给无线模块残留字节补上端到端动作 ID。
- `P` 的 `IDLE` 是底盘软件状态，不是外部测得的车轮静止；失效无线链路不能由 PC 保证机械停车。
- `ENC/DX/DY/DR/DS/Q1~Q4` 反映编码器执行，不证明没有滑移或已经达到真实空间位移。
- 真实速度、制动距离、模块残留/重发、DTR/RTS 复位行为、三路无线隔离、雷达安装外参和实车闭环均需现场测量。
