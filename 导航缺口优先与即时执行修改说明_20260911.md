# 导航缺口优先与即时执行

本轮继续修改根目录正式程序，覆盖上一轮说明中“每次停车后等待两圈”的已建图情况。未连接串口、未驱动车辆。重启 `start_hardware_navigation.bat` 生效，无需重新烧录。

## 1. 已有地图后不重复确认墙面

原流程在每次底盘动作后清空两圈墙面确认状态，即使地图已经建立、定位匹配良好，也必须再等两圈。现在保留建图用的两圈证据累积，但不再用它阻塞已建图后的导航动作：

- 已建立地图时，首个停车后的完整扫描立即参与匹配和规划。
- 当前完整扫描能检查动作空间、但全局匹配暂时失败时，直接尝试当前扫描的局部约 10 cm 动作，不额外复测两圈。
- 初次启动、尚未建立地图时仍使用两圈初始化；没有有效新扫描、底盘结果不确定或动作空间确实不足时仍不运动。
- 当前扫到的墙面证据只写一次，快速路径不会重复累计上一圈的墙面。

## 2. 删除距离控制模式中重复叠加的距离余量

原规划把车体半径、固定 12 cm 余量、名义速度乘数据年龄、停止距离及额外边距叠加，并用矩形包围区域检查，因此可能把“前方墙距 35 cm，只前进 10 cm”的动作拒绝。

现在对实际采用的距离控制 MOVE，检查车体圆沿有限行程扫过的区域（胶囊形），保留 3.5 cm 几何余量、测量角度/距离不确定度，以及配置中实际测得的速度上界和停止距离。去掉不适用于该模式的固定 12 cm 叠加和名义速度外推。实时避障检查同步采用相同的有限行程几何，避免规划放行、启动后立即被旧判据拦停。

速度控制模式保留原检查方式。地图边界、车体扫掠空间、真实障碍回波、执行反馈和新扫描边界检查保留。

## 3. 大于 40 cm 的未探索缺口优先于停车

从拟合墙面端点之间的断口检测物理宽度，要求宽度大于 40 cm，且缺口后方包含未知区域或尚未实测确认的假设空闲区域。若后方已经完整探测，或中间被已知墙阻断，不将它作为这类优先目标。

存在该类缺口时：

1. 清除终点确认计数和原有泊车目标，优先处理缺口。
2. 目标不在车头方向时先旋转对准，再按约 10 cm 步长进入。
3. 即使当前缺口方向没有可执行动作，也显示“缺口待通行”，不因多扫几圈而判为泊车完成。
4. 这类探索不受旧的单向路线进度限制阻挡，侧后方的未探索缺口同样可以成为目标。

停车的几何确认入口也检查未探索缺口，避免绕过主规划优先级。

## 4. 旋转优先于侧移和斜移

不仅是原有小角度绕行：缺口目标、普通路径以及空隙选择，都优先旋转对准目标。大角度朝向变化拆为每段不超过 25° 的已接入标定的旋转，每段完成后用首个新完整扫描重新判断。正前方目标直接前进，不做多余旋转。旋转空间不足时再尝试安全平移。

沿用前轮接入的 R/F 两点角度标定和停车补偿，不修改固件，不重发已登记的 MOVE。急停避障恢复仍优先处理真实近障碍。

## 配置与验证

`navigation_config.json` 已开启 `hardware_immediate_navigation`、`hardware_prioritize_unexplored_gaps`，并继续开启 `hardware_detour_rotation`。

新增 `tests/test_exploration_priority.py` 的 12 项测试均通过，覆盖：真实地图与执行位姿更新后首圈即动作、缺口压过停车/直行、30 cm 缺口不触发该优先级、已探索区域不误判、缺口受阻不停车完成、多段旋转后前进、普通侧向路径先旋转、35 cm 前墙下 10 cm 行程放行及 25 cm 障碍拒绝、实时与规划检查一致、实际配置开关。

完整回归执行 490 项：480 通过、9 跳过、1 项已有固件 CRLF/LF 文件哈希校验失败。输出：`tmp/exploration_priority_tests.txt`。该失败与本轮无关，未修改固件或哈希清单。

截图不是原始扫描回放，本轮通过对应几何场景和既有自动化用例验证，实际场地效果仍需重启后观察。
