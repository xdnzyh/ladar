# 前方缺口直行、墙面更新和停车恢复

## 本次定位

离线测试复现了三类问题：前方缺口偏离中心十几度就持续触发旋转；拟合成功的墙面仍因原始测距误差重复降权，迟迟未达到地图占用阈值；转向近障停车漏接自动避让恢复流程。另确认累积的“假设空闲”会拖慢新墙显示，定位失败后的局部规划会绕过避让请求。

截图不能单独证明工作线程死锁。本次修正的是能够在代码中复现的地图证据和动作决策问题，没有连接实车或发送底盘指令。

## 行为变化

- 大于 40 cm 的未探索缺口继续优先于停车；多个缺口中优先考虑前方约 ±25° 的缺口。
- 前方缺口的 20 cm 完整车体扫掠路径通过地图、原始雷达和底盘步长检查时，直接前进最多 20 cm，避免为了对准几度而左右转。路径不够时仍尝试小角度旋转或更短的安全平移。
- 墙面保留最短 20 cm、拟合 RMS 容限 5 cm；已通过连续点和直线检查的墙面，写入占用证据时不再重复按原始测距误差降权。原始测距误差仍用于运动间距检查。
- 新测到的墙面不必抵消多圈“假设空闲”的负权重。真实测量得到的空闲证据仍按正常累计处理。测试中的高质量新墙能在两次观测后进入占用地图；这不是对所有实车扫描的固定两圈承诺。
- 自动转向时雷达触发的近障停车，现在与自动平移近障停车一样，等待匹配的底盘停止结果、停稳和新扫描，然后选择让障碍距离增大的平移，优先远离最近障碍。恢复步长保持最多 5 cm，再重新扫描规划。
- 定位匹配失败时，局部扫描规划同样优先处理避让请求。
- 手动紧急停止保持停止；停止结果不明确或反方向空间不足时，不盲目恢复运动。

## 文件与验证

主要修改 `navigation_core.py`、`navigation_app.py`，回归用例在 `tests/test_exploration_priority.py` 和 `tests/test_board_course_navigation.py`。

执行 `py -3 -m unittest discover -s tests`；结果保存于 `tmp/forward_gap_recovery_tests.txt`。现有固件清单校验存在 CRLF/LF 导致的哈希差异，本次未修改固件。

重启电脑端导航程序加载新代码，无需因本次修改重新烧录固件。实车运动距离、打滑和雷达扰动效果仍需现场验证。
