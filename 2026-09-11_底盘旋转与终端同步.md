# 2026-09-11 底盘旋转与终端同步

本次在GitHub main提交`1e5ac66`基础上合并底盘现场更新。正式底盘目录为根目录`control/`，主入口仍为`start_hardware_navigation.bat`。现有雷达采集、显示、地图、路径与停车准入实现沿用main。

## 可以直接使用的功能

打开`control/参数工具/底盘集成终端.exe`，连接实际底盘串口。独立终端连接后直接可用，参数读取和应用由操作者主动触发，空闲PING预备握手保留。

- 平移继续使用毫米或CNT。
- 旋转默认30°，输入角度后点R顺时针/F逆时针；可切换CNT，独立记忆角度/CNT输入。
- “旋转标定/补偿”页可修改四个角度锚点与R/F提前量，点“保存到电脑”立即用于后续运动，无需烧录或写入车端。
- 运动仍只发一次受编号/CRC保护的MOVE。截断RESULT用同编号只读查询恢复，结果不确定和停止状态继续互锁，不重发运动。

日志能力标记：`ROTATION_CNT=1 ROTATION_BRAKE=1 ROTATION_DEG=1 MANUAL_CONFIG_SYNC=1`。度数是电脑入口单位，发送给Arduino的仍是CNT。

## 配置文件与断电行为

| 文件 | 用途 | 如何生效 |
|---|---|---|
| `control/参数工具/底盘参数.json` | 95项执行参数与8方向距离系数 | 执行参数需应用车端；距离系数在电脑使用 |
| `control/参数工具/旋转停车补偿.json` | schema2：补偿开关、R/F提前量、4个实测角度 | 独立终端保存到电脑即可，重开终端保留 |

本次执行参数CRC为**48822**。LONG速度目标快/中/慢/起步48/40.8/33.6/24；LAT、DIAG、ROT为38.4/33.6/28.8/24。ROT前馈快/中/慢135/130/125、起步150、过渡500ms、PWM90～180，与LAT同档。速度单位CNT/20ms。八方向距离系数保持现场值，尚不代表最终装配下所有方向都已重复验证。

Arduino执行参数保存在RAM，断电/重启恢复固件启动值。要继续使用这档动力参数，重新开电后主动应用一次。电脑端角度标定和停车补偿保存在JSON，无需每次重新填写。单独关闭终端而车未断电，不会清掉车上参数。

**导航配置已修正为读取根目录`control/参数工具/底盘参数.json`。** 原main的显式路径指向`car control final/car control final/control/参数工具/底盘参数.json`，会继续使用旧副本。本次将主参数路径及8方向来源路径一并切换，实际`load_configuration('hardware','navigation')`读取CRC48822。

Python导航仍采用自己的参数核对流程。独立终端“无需参数同步即可手动运动”的选择不自动放宽导航准入。两个程序不能同时占用同一底盘COM口。

## 角度换算与补偿语义

当前使用补偿开启后的人工实测锚点：

| 方向 | 最终目标500CNT | 最终目标1000CNT | 提前量 |
|---|---:|---:|---:|
| R | 20° | 50° | 145CNT |
| F | 20° | 47° | 145CNT |

角度θ不超过小档角度a时，终点目标`G=round(500×θ/a)`；其他角度使用`G=round(500+500×(θ−a)/(b−a))`，b为大档角度。实际下发`N=G−145`。例如R90°→G1667→N1522，F90°→G1796→N1651。

两档之间插值，未测范围估算并记录`EXTRAPOLATED=1`。终点非正、越界或扣提前量后非正均不发送，不静默夹成一个小动作。关闭补偿可以做原始CNT测试；度模式要求开启补偿，避免混用两套标定条件。

协议ACK/RESULT中的REQ保持实际下发N，按N校验；日志另记`INPUT/UNIT/GOAL_CNT/WIRE_CNT/LEAD`和`STOP_ERROR=ENC−G`。只从编码器获得完成结果，界面不宣称已经测得真实角度。

**Python自动导航尚未接入这套两点角度换算。** 当前`chassis_rotation_capabilities`仍未验证且关闭；不要仅填一个`counts_per_rad`就认为与独立终端等价，两点关系包含偏移且小角度分段。后续若接入导航，应在目标到wire的适配层实现同一映射，协议仍按wire目标核对，补偿只扣一次，并继续处理旋转规划、扫掠空间和停车后重定位。

## 已取得的现场证据

未补偿时，原请求500/1000的最终编码器比请求多约135～166CNT。补偿首轮结果如下：

| 方向 | 终点目标 | 最终ENC | 停车误差 |
|---|---:|---:|---:|
| R | 500 | 475.75 | −24.25 |
| F | 500 | 473.75 | −26.25 |
| F | 1000 | 1024.25 | +24.25 |
| R | 1000 | 985.25 | −14.75 |

后续补测误差−23.75～+16.25CNT，按现场“允许小幅误差”的要求保留145/145。真实角度记录与重复动作明细见`control/标定/20260911_旋转角度与停车记录.md`。直接输入度数的功能已完成离线测试，90°等实际精度尚待验证。

通信仍见过一次“无ACK、后续有效N回复”的动作未登记现象，其现场是否运动未确认；不能把RESULT恢复等同于所有指令丢失都已解决。第四路编码器此前近零，维修后的前进已有745/729/736/743这类四轮正常量级记录；个别侧移/斜移仍有轮间差异，需结合实车效果判断。

## 合并与验证范围

- 更新独立终端、命令行R/F CNT入口、共享R/F轮向校验及配套测试；两份exe已从本次源码重新构建。
- 同步动力配置、旋转配置、使用说明、过程记录和旋转标定记录。
- 导航只调整正式参数文件路径和相应集成测试；main已有雷达/导航功能代码未替换。
- 八个Arduino文件与现场源码按换行归一后完全一致，本次未修改；无需重新烧录。
- 集成终端与命令行离线测试通过；Python配置、底盘接口、RESULT恢复、空闲握手及底盘事务仿真相关**106项测试通过**；导航实际配置读取确认CRC48822。

验证未连接串口、未驱动车辆、未烧录。历史重复项目目录保留，使用与后续合并应以根目录正式代码和配置为准。
