# 串口自动导航实测

2026-09-11，程序接口启动实物导航，运行约45秒，结束后收到底盘IDLE及两端停止确认。未使用界面自动化。端口：测距COM8、旋转COM9、底盘COM11，已由正常关闭流程保存。

- 双端校时及真实零位组圈恢复，旋转周期约1.51秒；首次闭合圈保留90个测距点。
- 使用现有55cm板宽、横3板、纵23板的赛道模型。当前仅有此前照片的缩略图及尺寸描述，本次未验证障碍物具体位置或终点泊车。
- 动作1：W 1291 CNT，完成反馈ENC=1354，换算前进约0.1886m；这是编码器估计，不是外部测量。
- 动作2：W 1435 CNT，收到ACK后进入近障停车分支。返回结果前缀损坏为“SULT,...”，未通过完整结果解析；随后IDLE确认停车。
- 缺陷：`chassis_stop_confirmed`仅记录状态，没有结束近障恢复等待。底盘idle而应用moving=true，扫描持续被移动门槛拒收。此状态持续至试验限时退出。
- 定位范围：`navigation_app.py`的`_safety_stop`和`chassis_stop_confirmed`分支，以及`chassis_controller.py::_handle_idle`清除pending的行为。缺失完整运动结果时不能把IDLE当作位移证明直接继续导航；应完善结果恢复或明确中止并报告定位不确定。
- 测距串口另有少量PIX前缀损坏，如“5IX”；尚未确认物理链路根因。

原始状态见states.jsonl，应用过程见runtime.log，底盘字节日志见根目录logs/chassis_serial.jsonl对应10:07:39—10:08:28记录。当前业务代码未针对此新发现修改；本次只完成测试和定位，不能声称全赛道通过。
