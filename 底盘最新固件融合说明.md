# 最新底盘与 main 导航融合

本次只从 `car control final/car control final` 接入底盘实现。根目录 `control/` 的八个 Arduino 工程文件与交付版本逐字节一致，没有修改已调试的运动、标定和刹车固件。参数工具及其共用 JSON/源码一同保留；历史备份和旧雷达程序未引入。

## 已接通的流程

`navigation_app.py` 当前导航规划 → `ChassisMotionAdapter` 按共用 JSON 换算一次 MM→CNT → 空闲 PING/PONG → 带编号和 CRC 的 MOVE → 校验 RESULT → 现有编码器位姿更新 → 停稳后的新扫描 → 下一导航动作。

- RESULT 丢失或损坏时只查询原动作编号，不重发 MOVE；重复回报不重复更新位姿。
- CONFIG1 读取/应用成功后缓存 95 项执行参数确认状态；重启、断连、停止或配置变化会使确认失效。
- Python 补齐交接中的配置恢复：I/G 最多补发两次且每次使用新查询标记；B/S 最多补发两次，保持原事务与参数；C 只提交一次，应答未确认时完整读取 95 项，完全一致才放行。明确拒绝、持续失败或重启均不放行运动。
- 界面新增“读取底盘参数”“重载并同步参数”。这两个操作保留当前地图、建图快照和地图版本。
- `radar_app.py`、`radar_core.py`、扫描采集模块、`navigation_core.py`、`mapping_policy.py`、`mapping_runtime.py`、雷达配置及地图文件均未修改。当前 COM6/COM7 和雷达安装参数保留。

## 使用

1. 使用原入口 `start_hardware_navigation.bat`。展开“串口与指令”，选择底盘实际 COM 口（9600、8N1）；当前配置中的底盘端口仍为空，避免猜测现场端口。
2. 连接后确认空闲状态和固件能力；现有现场参数入口中对应能力为 `mm_ping_v1`。原有自动导航的各方向范围、速度/制动和不确定度检查继续有效，交付文件未提供的数值没有编造。
3. 点击“重载并同步参数”，等待日志显示“与电脑不同项 0”。参数保存在底盘 RAM，断电后需再次核对/同步。
4. 按现有导航流程获取有效扫描，再开启自动导航。参数尚未确认时导航预检会明确提示同步。

人工与自动运动共同使用 `control/参数工具/底盘参数.json`。独立工具修改该文件后，在主程序停止导航并确认底盘空闲，再点击“重载并同步参数”。两种程序不能同时占用一个底盘串口。

保留交付文件实际值 `LAT_RAMP_MS=600`（固件默认 500），参数 CRC=44698；这是交接中记录的测试值，未擅自改为新的调参结论。八方向距离系数也原样保留。主程序已开启 `chassis_result_recovery`、`chassis_idle_preflight`，使用 CNT 和 32 个前导空格。

## 验证记录

- 根目录及测试共 63 个 Python 文件完成语法解析；新增测试后已重新验证相关文件语法。
- 最终 146 项定向逻辑回归全部通过，覆盖配置恢复、运动结果恢复、空闲握手、当前规划器闭环、等待扫描门控、参数重载保图，以及雷达显示/墙面采集/建图策略回归。
- 合并期间完整 unittest 执行 374 项：364 通过、9 跳过、1 项原有雷达基线哈希失败。已核实两个雷达基线文件在 Windows 检出为 CRLF；转换为 LF 后 SHA256 均与原 manifest 完全一致，内容也与 HEAD 一致。本次没有改动这些文件或清单来掩盖该问题。
- `git diff --check` 通过。未运行长时间仿真、实车运动或烧录；软件闭环测试不等于装车后的自动导航验收。
