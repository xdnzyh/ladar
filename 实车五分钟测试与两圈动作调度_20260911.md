# 修改和实车测试记录

## 后续修正：两圈调度最高优先级

原实现只在普通规划返回停止后兜底，且定位失败、避让失败存在提前返回路径。现已修正：

- 在普通缺口规划、终点确认之前检查两圈预算；到第二圈首先选择动作。
- 定位拒绝分支调用相同的两圈动作入口，避免继续进入普通局部探索。
- 已请求避让但避让失败时，继续尝试前方半平面的 10 cm 平移、旋转及其他避让方向。
- 全局地图不确定导致动作受阻时，使用当前扫描构建一次性车体局部地图重新检查动作，不要求全局定位先恢复。原始近障检查仍有效；局部地图不写回全局地图。
- 建图线程统一执行这一优先级并标记当前完整扫描可用于动作准入，不把动作获准伪装为定位成功。计数仍在运动先验/执行结果更新时重置。
- 手动停止或设备故障不自动恢复；如果所有动作确实都不满足车体空间检查，则仍停车。

新增 5 项回归测试，完整测试记录为 `tmp/two_scan_highest_priority_tests.txt`。本节修改仅经过离线验证，下面的五分钟实车数据采集于本节修改之前。

## 最终修改

- 连续直线墙面保持 20 cm 最短长度、5 cm 拟合 RMS 容限。拟合质量至少 0.9、定位建图置信度至少 0.55 时，墙端点一次更新达到占用阈值，覆盖旧空闲证据；低质量点不使用这个快速确立规则。启动阶段仍有原来的首两圈准备。
- 自动避让短步最多 5 cm：允许从额外舒适余量中持续远离障碍，同时保留实际车体半径、测量误差、地图扫掠与新增障碍检查。规划和实时雷达保护均识别该类避让。
- 转向以缺口两端的实际空间中点为目标，而不是角度平均。沿用现有标定的 10–25° 单步范围。目标在移动 15 cm 前保持，且每圈必须仍有方向接近的真实缺口支持，避免仅因宽度波动左右切换。
- 缺口动作全部受阻时，继续尝试安全的短步平移避让。
- 自动导航在同一位置处理第二圈完整扫描后，如果正常规划仍没有动作，依次尝试前方 180° 内 10 cm 平移、20° 旋转和紧急短步避让。界面显示“两圈短步探索”“两圈转向探索”或具体拦截原因。不会通过碰撞检查失败的动作；手动急停、停车完成和设备故障仍保持停止。
- 修复实车发现的停止回应竞态：同一停止事务已经收到完整匹配结果后，迟到的一条清理 `BAD_CMD` 不再中断导航。限定事务、状态和时间窗口，其他指令错误仍按故障处理。

## 实车测试

使用配置中的 COM8 测距、COM9 雷达旋转、COM11 底盘，运行正式导航应用及原有握手、停止和结果确认流程。

首次测试运行 51.64 秒，第二次近障停车时出现停止清理回应竞态而提前结束。保存于 `logs/hardware_capture_20260911_132906`，没有把它计为完成五分钟。

修复回应竞态后，第二次测试持续 **300.94 秒**，到时请求停止，随后底盘进入 idle，记录程序正常关闭。

| 记录项目 | 第二次测试 |
| --- | ---: |
| 带扫描数据的建图结果 | 137 |
| 首次出现占用墙面的建图扫描序号 | 2 |
| 最大占用网格数 | 237 |
| 动作请求 | 12 |
| 旋转请求 / 平移请求 | 9 / 3 |
| 其中自动避让请求 | 1 |
| 雷达近障停车 | 2 |
| 建图处理异常 | 0 |

测试仍出现较长“缺口待通行”、定位不可信和左右转向。不能据此宣称导航已流畅。这些记录促成了目标保持、缺口受阻平移兜底和“两圈动作调度”的后续修改；**这三项只完成了离线回归，尚未再做实车测试**。137 次建图结果表明线程持续处理，但不能证明每面实物墙都已正确建成。

## 数据和复现

- `logs/hardware_capture_20260911_133512/events.jsonl`：扫描点、建图结果、姿态、匹配分数、动作请求、停车原因、周期状态和配置。
- `logs/hardware_capture_20260911_133512/chassis_serial.jsonl`：按本次时间范围抽取的底盘原始串口收发记录。
- `logs/hardware_capture_20260911_133512/summary.json`：统计结果；状态采样次数不等于精确持续秒数。
- `tools/hardware_navigation_capture.py --seconds 300`：真实设备测试入口，会主动运行小车，最长允许 300 秒。可在应用界面随时手动紧急停止；连接、预检失败或导航主动停止时提前结束，不自动绕过故障重启。
- `tools/replay_capture_geometry.py`：离线重建几何检查，仅作诊断，不等同完整运行时地图快照，不访问串口。
- `tmp/wall_escape_final_tests.txt`：最终完整回归结果。固件清单的既有 CRLF/LF 哈希差异仍未改动。

重启电脑端导航程序加载最终代码。本次没有改写或烧录固件。
