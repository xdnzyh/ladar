# 导航卡死取证

从根目录 `start_hardware_navigation.bat` 重新启动后，诊断自动启用，不需要额外点击。

- `logs/navigation_日期_时间.log`：保存界面状态消息、BAD_CMD 等底盘故障和 Tk 回调异常。
- `logs/navigation_日期_时间_stacks.log`：界面连续 8 秒未执行心跳时，由独立原生看门狗保存所有 Python 线程堆栈。正常运行时文件可以为空。
- `logs/chassis_serial.jsonl`：现有底盘串口收发记录；当前导航配置已开启。包含电脑实际发送的命令、底盘回复和动作编号。

再次发生按钮无响应时，在现场安全允许的情况下保留进程至少 10 秒，让堆栈写入完成。界面卡死时不要依赖界面急停按钮，必要时使用硬件急停或切断底盘动力。结束后保留整个 logs 文件夹，并记录故障发生时间。

这些改动只用于取证，不会自动重发运动命令，也不代表 BAD_CMD 或卡死已修复。日志会持续追加，请在故障分析后按需归档。
