<<<<<<< HEAD
# 同步建图检查与烧录说明

## 结论

原程序不能保证准确建图。测距采样时刻、光电零位时刻和电脑接收时刻混用，两条无线链路的不同延迟会变成角度误差。目前保存的距离标定也是空的，尚不能用现有配置把中心像素转换为实测距离。

配套程序沿用手册接线：旋转端在 GPIO 中断内记录时间，测距端记录单次采集时间区间，电脑通过双向校时统一时基，再经共用观测接口重排并组装完整扫描。它修正软件错配，但不能替代实物精度验证；每圈只有一个光电零位，仍然无法测出圈内角速度变化。

## 烧录与使用

| 位置 | 文件 | 操作 |
| --- | --- | --- |
| 旋转机构 ESP32 | `esp32_rotation_sync/esp32_rotation_sync.ino` | Arduino IDE 打开，选择 ESP32 Dev Module 和对应 USB 端口，编译上传。该板改用 Arduino 固件，不再运行原 MicroPython `main.py`。 |
| 测距机构 ESP32 | `esp32_measurement_sync.py` | 保留 MicroPython 环境，通过 Thonny 保存到设备，文件名为 `main.py`，重新启动该板。 |
| PC 标定 | `start_radar.bat` | 连接设备，进入“标定”，使用“读取当前像素”；不要点击旧界面的“开始扫描”。 |
| PC 同步扫描及建图 | `start_hardware_navigation.bat` | 启动 `hardware_app.py --view radar`，默认使用两端同步固件。 |

两块 ESP32 分别烧录对应文件；不要继续用 `esp32_rotation_precise.py` 配合新版同步入口。旋转端已经按本机 ESP32 Arduino Core 3.3.0、`esp32:esp32:esp32` 编译通过。若 Arduino 工具链因中文路径链接失败，把整个旋转 sketch 文件夹复制到英文路径后编译。

本次续接版本的测距固件为 `MEASUREMENT_SYNC_V2`，必须与更新后的 PC 标定程序一同升级。旧测距固件不支持带请求编号的标定命令，会拒绝新标定请求；请重新把 `esp32_measurement_sync.py` 保存到测距板的 `main.py` 并重启。旋转端同步固件本次没有修改，已烧录该文件的旋转板无需重复升级。

### 接线

接线依据《指导书》第 10–11、16 页，没有增加跨旋转轴的信号线。

| 模块 | ESP32 引脚与参数 |
| --- | --- |
| 旋转继电器、光电开关 | GPIO13 高电平开启；GPIO27 下降沿触发 |
| 旋转无线模块 | TX17、RX16、AUX26、MD0=33、MD1=32；115200、8N1 |
| 测距 CCD、激光 | CCD TX17、RX16，256000、8N1；激光 GPIO25 |
| 测距无线模块 | TX4、RX5、AUX18；115200、8N1；MD0/MD1 按手册接低电平 |

115200 是本地串口速率，不是已验证的无线净吞吐率。各链路的成对模块配置匹配，不同链路避免同信道串扰。

### 标定与首次扫描

1. 烧录两端程序，保持小车静止。打开 `start_radar.bat`，选择两个不同 COM 口并连接。
2. 在“控制”页设置曝光和中心像素格式。`fffe` 对应 `@c0071#@`，按 `FF FE + 高字节 + 低字节` 解析；另附 TCD1103 资料明确说明 `@c0081#@` 返回两个字节，对应 `raw2`。资料包含不同版本，必须匹配实物。如果 `fffe` 超时，先核对实际响应再选择 `raw2`，不能靠改标定系数掩盖格式错误。
3. 在“标定”页点击“读取当前像素”，确认显示更新且重复读数稳定，再点击“添加当前点”输入实际距离。每次请求使用独立编号，只接受本次请求在 2 s 内到达的一帧读数，固件完成单帧采集后关闭激光。至少采集 6 个覆盖实际量程的距离位置，点击“拟合并保存”。取点期间曝光、焦距和安装保持一致，不需要启动旋转机构。
4. 关闭标定程序，释放 COM 口；打开 `start_hardware_navigation.bat`，先选“仅雷达”。新入口优先继承有效雷达标定及其曝光、CCD 格式。
5. 点击“开始”。两端分别完成 8 次双向校时，PC 发送测距启动命令；只有收到匹配本次会话的 `OK START`，才发送旋转启动命令；收到匹配的 `OK ROT` 后才进入扫描处理。任一启动阶段超过 2 s 未获确认或发送失败，停止两端并报错。首个零位只建立起点，随后要求连续两次圈周期变化不超过 5%；首个有效扫描至少需要 4 个零位。未获得实测周期时显示“—”。
6. 时间配准误差、有效点不足、角度空缺等原因显示在右侧。超限扫描不入图。不要只增大阈值来把“没有有效扫描”变成“准确扫描”。
7. “仅雷达”显示局部扫描，不累计环境地图。“自动导航”仍需实际底盘四轮指令和停止指令；当前配置为空，不能直接控制底盘建图。应先完成静止扫描验证。

## 原程序问题与处理

| 问题 | 依据及影响 | 当前处理 |
| --- | --- | --- |
| 时间戳格式不兼容 | 精确版旋转固件发 `TRIG n TICK_US=...`，原建图解析器只收纯数字时间戳。 | 兼容旧格式；新入口使用带会话号的统一格式。 |
| 转速报告被当作零位 | 原 `radar_app.py` 把 `SLIDE_REPORT WINDOW=1 CYCLE[...]` 的电脑接收时刻传给 `trigger()`。 | 删除这种零位替代逻辑。当前目录没有该报告的生产端代码，只能确认消费端行为。 |
| 单向拟合不是真正校时 | 原 `DeviceClock`/`DeviceClockMapper` 的偏移包含各链路固定延迟；低延迟样本拟合也无法消除独立链路的偏差。 | 双向交换，保留不确定度；扫描期间固定映射。 |
| CCD 无采样时间 | `CCD.docx` 仅透明转发，电脑接收时间包含采集、UART、无线和线程延迟。 | 测距端封装采集区间与序号，只允许一个未完成的采集请求。 |
| 大块串口读取产生额外等待 | 原 `read(4096)` 配合 50 ms 超时，多帧可能共用一次回调的时间。 | pySerial 和 Win32 后端均按可用字节读取，无数据时等待首字节。 |
| 固定排序窗口不保证数据齐全 | 原建图入口使用 80 ms 窗口，超时到达的测距可能跨圈。 | 同步入口按两路最新事件共同推进，零位等待测距流越过边界。 |
| 丢零位被当成慢转一圈 | 原 `RotationTracker` 不检查圈号连续性，从 1 跳到 3 仍转换为一圈。 | 缺帧、时间倒退、设备重启使当前扫描失效；重复序号不重复处理。 |
| 中断事件可被覆盖 | 精确版延迟回调读取可变的 `pending_count/pending_tick_us`，可能已经对应下一事件。 | 推荐旋转固件使用独立环形队列记录各次中断，前台发送。 |
| 微秒计数回绕 | 原映射将 `ticks_us` 当作无限增长时间。 | 旋转端使用 64 位计时；测距端用 `ticks_diff()` 连续展开时钟。 |
| 启停残留数据跨会话 | 原启停没有会话标记。 | 每次扫描使用新会话号，忽略旧数据；运动期间停止采集。 |
| 空标定覆盖有效标定 | 原导航配置中的空标定覆盖雷达标定。 | 优先继承有效雷达标定。 |
| 缺测区被插值补成障碍 | 原加密只检查距离差，缺少角度上限。 | 超过 15° 的空缺不加密；同步扫描另查整体覆盖。 |

## 转速、角度与时间配准

推荐旋转固件发送每次光电零位的中断时间。电脑按设备事件计算周期和转速：

```text
T = t_zero[n+1] - t_zero[n]
RPS = 1/T
RPM = 60/T
theta(t) = theta0 + direction * 2*pi*(t-t_zero[n])/T
x = distance*sin(theta)
y = distance*cos(theta)
```

如果 `AVG_RPS` 的生产端确实按每秒转数定义，需要乘 60 才是 rpm。但平均转速没有零位相位信息，不能单独恢复角度；滑动窗口平均值也不能作为本圈每一点的实际角速度。其完整生产端不在当前资料中，不能确认窗口实现。

假设转动周期为 2.5 s，50 ms 的相对时间误差对应 7.2°；距离 2 m 处的位置偏差约 25 cm。这里是计算示例，不是实测转速。即使相邻圈周期一致，圈内仍可能存在周期性速度变化。

电脑在串口写线程实际写出前记 `h1`；设备收到完整命令记 `d2`、即将发送应答记 `d3`；电脑收到完整应答记 `h4`：

```text
SYNC id d2_us d3_us
offset(device-host) = ((d2-h1)+(d3-h4))/2
host_time = device_time-offset
network_RTT = (h4-h1)-(d3-d2)
```

程序选择本轮最小往返时间的估计，并保留约半个网络往返时间的不确定度，再增加时钟漂移预算。无线双向路径未必对称，不能把中点估计当成精确时钟偏移。

```text
START session sample_hz exposure_index fffe|raw2
OK START session
PIX session sequence capture_begin_us frame_end_us pixel
ROT session
OK ROT session
TRIG session count zero_time_us
CAL request_id exposure_index fffe|raw2
PIX request_id 1 capture_begin_us frame_end_us pixel
STOP
OFF
```

标定请求编号由 PC 每次重新生成，不能重复使用。测距固件保留旧的 `CAL exposure_index mode` 单次采集格式供旧程序使用，但新版 PC 不接受没有匹配请求编号的返回。校时、启动确认和样本帧都按设备来源与本次会话识别；重复的启动确认不会再次启动电机。

采集区间从 ESP32 发出 CCD 请求前，到完整中心坐标返回后。程序用区间中点估计采样时刻，把半区间计入误差；没有把收到坐标的时刻宣称为曝光中心。包络依赖“一条读取命令对应新采集帧”，实物必须核对。如果 CCD 返回旧缓存帧，应取得曝光/帧同步信号或重新测量其采样延迟。

每点合并采样、前后零位和校时误差，再换算为横向位置误差。默认 0.04 m 只是时间配准预算，不包括距离误差、非匀速、机械偏心和底盘定位误差，不能理解为整张地图精度达到 4 cm。

### 软件参数

以下字段可在 `navigation_config.json` 设置；未写出时采用默认值。它们不是硬件实测结论。

| 字段 | 默认值 | 含义 |
| --- | --- | --- |
| `synchronized_acquisition` | `true` | 配套同步模式 |
| `arbitrary_phase_scans` | `true` | 默认实物导航与模拟共用任意相位组帧 |
| `observation_reorder_s` | `0.3` | 观测重排窗口，需覆盖实测无线延迟 |
| `hardware_settle_s` | `0.2` | 底盘停止命令发出后的等待时间，需实测停车残余运动 |
| `hardware_sample_rate_hz` | `20` | 本地目标请求频率，实际频率受 CCD、曝光和无线吞吐限制 |
| `exposure_index` | `8` | 手册表约 3.2 ms；有有效标定时继承其曝光 |
| `max_timing_position_error_m` | `0.04` | 时间配准位置误差预算 |
| `clock_drift_bound_ppm` | `500` | 待实测确认的时钟漂移预算 |
| `irq_timestamp_uncertainty_ms` | `2.0` | 待实测确认的边沿至软件记录时间预算，并非宣称实际中断延迟为 2 ms |
| `period_tolerance` | `0.05` | 相邻圈周期变化阈值 |
| `max_scan_gap_deg` | `25` | 最大未覆盖角度 |
| `sync_scan_duration_s` | `30` | 最长单次扫描时间，随后自动停止并重新校时 |

两端超过约 40 s 没有控制命令会停止本端执行器。电脑端检查测距 2 s、零位 10 s 未到达的情况。真实转动过慢时需要依据实测调整等待策略，不能把配置周期当作实测周期。

## 进一步提高实物精度

1. **增加角度观测。** 编码器或多个已知角间距的光电标记可缩短匀速假设区间；现有继电器加单零位无法闭环稳速，也无法恢复圈内抖动。
2. **验证同步误差。** 测量无线往返时间、非对称性和丢包。若始终不满足预算，采用两端共同捕获的同步信号，或让测距端直接记录角度基准；旋转轴接线/滑环条件另行确认。同时发 ON、同时清零或固定减去未知延迟都不能替代同步。
3. **检查 CCD 与光学。** 核实读取新帧、曝光、饱和和信噪比；使用覆盖量程的实测标定。`d=k/(pixel-p0)` 在像素接近 `p0` 时会放大噪声，3 m 默认量程不是精度证据。
4. **补充机械外参。** 当前坐标转换默认测距原点与旋转轴重合。测量激光/接收光路相对旋转轴、雷达相对车体中心的偏移与方向，再加入外参变换；未知参数没有填入虚构值。
5. **补齐移动定位。** 现有导航主要按 `predict_motion(command)` 预测位移；底盘状态仅保存，未形成编码器里程计融合。局部匹配窗口约 ±8 cm、±4°，不是完整回环 SLAM。应实测四轮输出与实际速度，再接编码器/IMU或外部位姿反馈。否则局部扫描正确也不保证多位置地图重合。
6. **验证真正停稳。** 默认底盘停止命令发出后的等待为 200 ms，缺少实际停车反馈。需确认惯性、振动和扫描期间静止状态。仅发送停止指令不等于已经停稳。
7. **检查场景适用性。** 稀疏点不能靠插值变成真实观测；长直走廊、细障碍、反光和遮挡需实测。单向通道的规划与终点判断不能直接推广到任意场地。

## 验收与依据

先在静止状态测直墙、直角墙和已知距离目标，比较多圈重合、直线残差和角点开角，同时记录周期、有效点数、校时不确定度。改变链路负载、曝光和扫描持续时间，复测结果。逻辑分析仪或示波器可核对光电边沿、CCD 请求/返回；若能取得曝光同步信号，应进一步核对真实曝光时刻。漂移和 IRQ 预算需实测支撑。

软件验证包含协议、双向校时、独立时钟和延迟链路回放、丢帧/重启/误差拒绝、测距固件模拟 UART、原有建图/导航回归，以及真实 Windows 窗口的标定按钮和扫描显示。窗口使用模拟数据，没有打开硬件串口。没有完成实物烧录、MicroPython 板上运行、无线时延或墙面精度测量。

### 2026-09-07 续接修复

在仓库提交 `996eeba` 基础上，先新增用例复现了 7 项失败，随后又复现了旧读数滞留界面队列的失败，完成以下修复：

- 标定超时后，旧响应原本会满足下一次标定，或进入原始二字节解析器成为伪像素。现按独立请求编号、响应到达时限及采集区间检查，标定 ASCII 数据不会在请求结束后回落到二进制解析。请求编号一直保留到界面更新，已进入队列的旧读数也不能覆盖新请求。
- CCD 完整帧若在超时后才被主循环读到，原本会先清除采集状态而绕过超时检查。现先检查 250 ms 采集区间，超时立即停止，不再转发迟到的同步帧或残留字节。
- 校时完成后，原本不检查启动发送结果，也不等待设备确认，就同时启动两端。现按测距确认、旋转确认顺序启动，检查发送失败、启动超时、旧会话及重复确认，并保留等待旋转确认期间已经到达的测距数据。

本轮使用 `python -m unittest discover -s tests -v` 验证，60 项测试通过，其中新增 17 项。覆盖 PC 与测距固件之间的真实命令/解析流程（UART 和时钟由测试模拟），以及上述异常场景。未重新进行上一轮记录的 Arduino 编译与 Windows 窗口检查；原硬件手册文件也未包含在当前 Git 仓库中，本轮未重新核验手册接线。实物烧录、板上运行、无线时延、距离标定和墙面精度验收仍需现场完成。

材料依据：`指导书.pdf` 第 2–3、10–18 页；`ESP32硬件/CCD.docx`；`ESP32硬件/Lora_moto_control.docx`；`读取光束中心点位置1103指令.txt`；小车运动控制指导书及 `utils.cpp/utils.h`。手册第 17 页提到 ARM，但所附电机程序没有 ARM 分支，应以所烧固件为准。小车资料使用 Arduino UNO 和驱动板，没有给出当前上位机可直接使用的四轮速度闭环协议。

技术依据：[pySerial 读取和超时](https://pyserial.readthedocs.io/en/latest/pyserial_api.html)、[MicroPython 计时回绕](https://www.micropython.org/resources/docs/en/latest/library/time.html)、[ESP32 GPIO 中断](https://docs.espressif.com/projects/arduino-esp32/en/latest/api/gpio.html)、[ESP Timer 微秒计时](https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/system/esp_timer.html)。
=======
# 同步建图检查与烧录说明

## 结论

原程序不能保证准确建图。测距采样时刻、光电零位时刻和电脑接收时刻混用，两条无线链路的不同延迟会变成角度误差。目前保存的距离标定也是空的，尚不能用现有配置把中心像素转换为实测距离。

配套程序沿用手册接线：旋转端在 GPIO 中断内记录时间，测距端记录单次采集时间区间，电脑通过双向校时统一时基，再经共用观测接口重排并组装完整扫描。它修正软件错配，但不能替代实物精度验证；每圈只有一个光电零位，仍然无法测出圈内角速度变化。

## 烧录与使用

| 位置 | 文件 | 操作 |
| --- | --- | --- |
| 旋转机构 ESP32 | `esp32_rotation_sync/esp32_rotation_sync.ino` | Arduino IDE 打开，选择 ESP32 Dev Module 和对应 USB 端口，编译上传。该板改用 Arduino 固件，不再运行原 MicroPython `main.py`。 |
| 测距机构 ESP32 | `esp32_measurement_sync.py` | 保留 MicroPython 环境，通过 Thonny 保存到设备，文件名为 `main.py`，重新启动该板。 |
| PC 标定 | `start_radar.bat` | 连接设备，进入“标定”，使用“读取当前像素”；不要点击旧界面的“开始扫描”。 |
| PC 同步扫描及建图 | `start_hardware_navigation.bat` | 启动 `hardware_app.py --view radar`，默认使用两端同步固件。 |

两块 ESP32 分别烧录对应文件；不要继续用 `esp32_rotation_precise.py` 配合新版同步入口。旋转端已经按本机 ESP32 Arduino Core 3.3.0、`esp32:esp32:esp32` 编译通过。若 Arduino 工具链因中文路径链接失败，把整个旋转 sketch 文件夹复制到英文路径后编译。

本次续接版本的测距固件为 `MEASUREMENT_SYNC_V2`，必须与更新后的 PC 标定程序一同升级。旧测距固件不支持带请求编号的标定命令，会拒绝新标定请求；请重新把 `esp32_measurement_sync.py` 保存到测距板的 `main.py` 并重启。旋转端同步固件本次没有修改，已烧录该文件的旋转板无需重复升级。

### 接线

接线依据《指导书》第 10–11、16 页，没有增加跨旋转轴的信号线。

| 模块 | ESP32 引脚与参数 |
| --- | --- |
| 旋转继电器、光电开关 | GPIO13 高电平开启；GPIO27 下降沿触发 |
| 旋转无线模块 | TX17、RX16、AUX26、MD0=33、MD1=32；115200、8N1 |
| 测距 CCD、激光 | CCD TX17、RX16，256000、8N1；激光 GPIO25 |
| 测距无线模块 | TX4、RX5、AUX18；115200、8N1；MD0/MD1 按手册接低电平 |

115200 是本地串口速率，不是已验证的无线净吞吐率。各链路的成对模块配置匹配，不同链路避免同信道串扰。

### 标定与首次扫描

1. 烧录两端程序，保持小车静止。打开 `start_radar.bat`，选择两个不同 COM 口并连接。
2. 在“控制”页设置曝光和中心像素格式。`fffe` 对应 `@c0071#@`，按 `FF FE + 高字节 + 低字节` 解析；另附 TCD1103 资料明确说明 `@c0081#@` 返回两个字节，对应 `raw2`。资料包含不同版本，必须匹配实物。如果 `fffe` 超时，先核对实际响应再选择 `raw2`，不能靠改标定系数掩盖格式错误。
3. 在“标定”页点击“读取当前像素”，确认显示更新且重复读数稳定，再点击“添加当前点”输入实际距离。每次请求使用独立编号，只接受本次请求在 2 s 内到达的一帧读数，固件完成单帧采集后关闭激光。至少采集 6 个覆盖实际量程的距离位置，点击“拟合并保存”。取点期间曝光、焦距和安装保持一致，不需要启动旋转机构。
4. 关闭标定程序，释放 COM 口；打开 `start_hardware_navigation.bat`，先选“仅雷达”。新入口优先继承有效雷达标定及其曝光、CCD 格式。
5. 点击“开始”。两端分别完成 8 次双向校时，PC 发送测距启动命令；只有收到匹配本次会话的 `OK START`，才发送旋转启动命令；收到匹配的 `OK ROT` 后才进入扫描处理。任一启动阶段超过 2 s 未获确认或发送失败，停止两端并报错。首个零位只建立起点，随后要求连续两次圈周期变化不超过 5%；首个有效扫描至少需要 4 个零位。未获得实测周期时显示“—”。
6. 时间配准误差、有效点不足、角度空缺等原因显示在右侧。超限扫描不入图。不要只增大阈值来把“没有有效扫描”变成“准确扫描”。
7. “仅雷达”显示局部扫描，不累计环境地图。“自动导航”仍需实际底盘四轮指令和停止指令；当前配置为空，不能直接控制底盘建图。应先完成静止扫描验证。

## 原程序问题与处理

| 问题 | 依据及影响 | 当前处理 |
| --- | --- | --- |
| 时间戳格式不兼容 | 精确版旋转固件发 `TRIG n TICK_US=...`，原建图解析器只收纯数字时间戳。 | 兼容旧格式；新入口使用带会话号的统一格式。 |
| 转速报告被当作零位 | 原 `radar_app.py` 把 `SLIDE_REPORT WINDOW=1 CYCLE[...]` 的电脑接收时刻传给 `trigger()`。 | 删除这种零位替代逻辑。当前目录没有该报告的生产端代码，只能确认消费端行为。 |
| 单向拟合不是真正校时 | 原 `DeviceClock`/`DeviceClockMapper` 的偏移包含各链路固定延迟；低延迟样本拟合也无法消除独立链路的偏差。 | 双向交换，保留不确定度；扫描期间固定映射。 |
| CCD 无采样时间 | `CCD.docx` 仅透明转发，电脑接收时间包含采集、UART、无线和线程延迟。 | 测距端封装采集区间与序号，只允许一个未完成的采集请求。 |
| 大块串口读取产生额外等待 | 原 `read(4096)` 配合 50 ms 超时，多帧可能共用一次回调的时间。 | pySerial 和 Win32 后端均按可用字节读取，无数据时等待首字节。 |
| 固定排序窗口不保证数据齐全 | 原建图入口使用 80 ms 窗口，超时到达的测距可能跨圈。 | 同步入口按两路最新事件共同推进，零位等待测距流越过边界。 |
| 丢零位被当成慢转一圈 | 原 `RotationTracker` 不检查圈号连续性，从 1 跳到 3 仍转换为一圈。 | 缺帧、时间倒退、设备重启使当前扫描失效；重复序号不重复处理。 |
| 中断事件可被覆盖 | 精确版延迟回调读取可变的 `pending_count/pending_tick_us`，可能已经对应下一事件。 | 推荐旋转固件使用独立环形队列记录各次中断，前台发送。 |
| 微秒计数回绕 | 原映射将 `ticks_us` 当作无限增长时间。 | 旋转端使用 64 位计时；测距端用 `ticks_diff()` 连续展开时钟。 |
| 启停残留数据跨会话 | 原启停没有会话标记。 | 每次扫描使用新会话号，忽略旧数据；运动期间停止采集。 |
| 空标定覆盖有效标定 | 原导航配置中的空标定覆盖雷达标定。 | 优先继承有效雷达标定。 |
| 缺测区被插值补成障碍 | 原加密只检查距离差，缺少角度上限。 | 超过 15° 的空缺不加密；同步扫描另查整体覆盖。 |

## 转速、角度与时间配准

推荐旋转固件发送每次光电零位的中断时间。电脑按设备事件计算周期和转速：

```text
T = t_zero[n+1] - t_zero[n]
RPS = 1/T
RPM = 60/T
theta(t) = theta0 + direction * 2*pi*(t-t_zero[n])/T
x = distance*sin(theta)
y = distance*cos(theta)
```

如果 `AVG_RPS` 的生产端确实按每秒转数定义，需要乘 60 才是 rpm。但平均转速没有零位相位信息，不能单独恢复角度；滑动窗口平均值也不能作为本圈每一点的实际角速度。其完整生产端不在当前资料中，不能确认窗口实现。

假设转动周期为 2.5 s，50 ms 的相对时间误差对应 7.2°；距离 2 m 处的位置偏差约 25 cm。这里是计算示例，不是实测转速。即使相邻圈周期一致，圈内仍可能存在周期性速度变化。

电脑在串口写线程实际写出前记 `h1`；设备收到完整命令记 `d2`、即将发送应答记 `d3`；电脑收到完整应答记 `h4`：

```text
SYNC id d2_us d3_us
offset(device-host) = ((d2-h1)+(d3-h4))/2
host_time = device_time-offset
network_RTT = (h4-h1)-(d3-d2)
```

程序选择本轮最小往返时间的估计，并保留约半个网络往返时间的不确定度，再增加时钟漂移预算。无线双向路径未必对称，不能把中点估计当成精确时钟偏移。

```text
START session sample_hz exposure_index fffe|raw2
OK START session
PIX session sequence capture_begin_us frame_end_us pixel
ROT session
OK ROT session
TRIG session count zero_time_us
CAL request_id exposure_index fffe|raw2
PIX request_id 1 capture_begin_us frame_end_us pixel
STOP
OFF
```

标定请求编号由 PC 每次重新生成，不能重复使用。测距固件保留旧的 `CAL exposure_index mode` 单次采集格式供旧程序使用，但新版 PC 不接受没有匹配请求编号的返回。校时、启动确认和样本帧都按设备来源与本次会话识别；重复的启动确认不会再次启动电机。

采集区间从 ESP32 发出 CCD 请求前，到完整中心坐标返回后。程序用区间中点估计采样时刻，把半区间计入误差；没有把收到坐标的时刻宣称为曝光中心。包络依赖“一条读取命令对应新采集帧”，实物必须核对。如果 CCD 返回旧缓存帧，应取得曝光/帧同步信号或重新测量其采样延迟。

每点合并采样、前后零位和校时误差，再换算为横向位置误差。默认 0.04 m 只是时间配准预算，不包括距离误差、非匀速、机械偏心和底盘定位误差，不能理解为整张地图精度达到 4 cm。

### 软件参数

以下字段可在 `navigation_config.json` 设置；未写出时采用默认值。它们不是硬件实测结论。

| 字段 | 默认值 | 含义 |
| --- | --- | --- |
| `synchronized_acquisition` | `true` | 配套同步模式 |
| `arbitrary_phase_scans` | `true` | 默认实物导航与模拟共用任意相位组帧 |
| `observation_reorder_s` | `0.3` | 观测重排窗口，需覆盖实测无线延迟 |
| `hardware_settle_s` | `0.2` | 底盘停止命令发出后的等待时间，需实测停车残余运动 |
| `hardware_sample_rate_hz` | `20` | 本地目标请求频率，实际频率受 CCD、曝光和无线吞吐限制 |
| `exposure_index` | `8` | 手册表约 3.2 ms；有有效标定时继承其曝光 |
| `max_timing_position_error_m` | `0.04` | 时间配准位置误差预算 |
| `clock_drift_bound_ppm` | `500` | 待实测确认的时钟漂移预算 |
| `irq_timestamp_uncertainty_ms` | `2.0` | 待实测确认的边沿至软件记录时间预算，并非宣称实际中断延迟为 2 ms |
| `period_tolerance` | `0.05` | 相邻圈周期变化阈值 |
| `max_scan_gap_deg` | `25` | 最大未覆盖角度 |
| `sync_scan_duration_s` | `30` | 最长单次扫描时间，随后自动停止并重新校时 |

两端超过约 40 s 没有控制命令会停止本端执行器。电脑端检查测距 2 s、零位 10 s 未到达的情况。真实转动过慢时需要依据实测调整等待策略，不能把配置周期当作实测周期。

## 进一步提高实物精度

1. **增加角度观测。** 编码器或多个已知角间距的光电标记可缩短匀速假设区间；现有继电器加单零位无法闭环稳速，也无法恢复圈内抖动。
2. **验证同步误差。** 测量无线往返时间、非对称性和丢包。若始终不满足预算，采用两端共同捕获的同步信号，或让测距端直接记录角度基准；旋转轴接线/滑环条件另行确认。同时发 ON、同时清零或固定减去未知延迟都不能替代同步。
3. **检查 CCD 与光学。** 核实读取新帧、曝光、饱和和信噪比；使用覆盖量程的实测标定。`d=k/(pixel-p0)` 在像素接近 `p0` 时会放大噪声，3 m 默认量程不是精度证据。
4. **补充机械外参。** 当前坐标转换默认测距原点与旋转轴重合。测量激光/接收光路相对旋转轴、雷达相对车体中心的偏移与方向，再加入外参变换；未知参数没有填入虚构值。
5. **补齐移动定位。** 现有导航主要按 `predict_motion(command)` 预测位移；底盘状态仅保存，未形成编码器里程计融合。局部匹配窗口约 ±8 cm、±4°，不是完整回环 SLAM。应实测四轮输出与实际速度，再接编码器/IMU或外部位姿反馈。否则局部扫描正确也不保证多位置地图重合。
6. **验证真正停稳。** 默认底盘停止命令发出后的等待为 200 ms，缺少实际停车反馈。需确认惯性、振动和扫描期间静止状态。仅发送停止指令不等于已经停稳。
7. **检查场景适用性。** 稀疏点不能靠插值变成真实观测；长直走廊、细障碍、反光和遮挡需实测。单向通道的规划与终点判断不能直接推广到任意场地。

## 验收与依据

先在静止状态测直墙、直角墙和已知距离目标，比较多圈重合、直线残差和角点开角，同时记录周期、有效点数、校时不确定度。改变链路负载、曝光和扫描持续时间，复测结果。逻辑分析仪或示波器可核对光电边沿、CCD 请求/返回；若能取得曝光同步信号，应进一步核对真实曝光时刻。漂移和 IRQ 预算需实测支撑。

软件验证包含协议、双向校时、独立时钟和延迟链路回放、丢帧/重启/误差拒绝、测距固件模拟 UART、原有建图/导航回归，以及真实 Windows 窗口的标定按钮和扫描显示。窗口使用模拟数据，没有打开硬件串口。没有完成实物烧录、MicroPython 板上运行、无线时延或墙面精度测量。

### 2026-09-07 续接修复

在仓库提交 `996eeba` 基础上，先新增用例复现了 7 项失败，随后又复现了旧读数滞留界面队列的失败，完成以下修复：

- 标定超时后，旧响应原本会满足下一次标定，或进入原始二字节解析器成为伪像素。现按独立请求编号、响应到达时限及采集区间检查，标定 ASCII 数据不会在请求结束后回落到二进制解析。请求编号一直保留到界面更新，已进入队列的旧读数也不能覆盖新请求。
- CCD 完整帧若在超时后才被主循环读到，原本会先清除采集状态而绕过超时检查。现先检查 250 ms 采集区间，超时立即停止，不再转发迟到的同步帧或残留字节。
- 校时完成后，原本不检查启动发送结果，也不等待设备确认，就同时启动两端。现按测距确认、旋转确认顺序启动，检查发送失败、启动超时、旧会话及重复确认，并保留等待旋转确认期间已经到达的测距数据。

本轮使用 `python -m unittest discover -s tests -v` 验证，60 项测试通过，其中新增 17 项。覆盖 PC 与测距固件之间的真实命令/解析流程（UART 和时钟由测试模拟），以及上述异常场景。未重新进行上一轮记录的 Arduino 编译与 Windows 窗口检查；原硬件手册文件也未包含在当前 Git 仓库中，本轮未重新核验手册接线。实物烧录、板上运行、无线时延、距离标定和墙面精度验收仍需现场完成。

材料依据：`指导书.pdf` 第 2–3、10–18 页；`ESP32硬件/CCD.docx`；`ESP32硬件/Lora_moto_control.docx`；`读取光束中心点位置1103指令.txt`；小车运动控制指导书及 `utils.cpp/utils.h`。手册第 17 页提到 ARM，但所附电机程序没有 ARM 分支，应以所烧固件为准。小车资料使用 Arduino UNO 和驱动板，没有给出当前上位机可直接使用的四轮速度闭环协议。

技术依据：[pySerial 读取和超时](https://pyserial.readthedocs.io/en/latest/pyserial_api.html)、[MicroPython 计时回绕](https://www.micropython.org/resources/docs/en/latest/library/time.html)、[ESP32 GPIO 中断](https://docs.espressif.com/projects/arduino-esp32/en/latest/api/gpio.html)、[ESP Timer 微秒计时](https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/system/esp_timer.html)。

>>>>>>> 2e7b899d23a3036e88a61b6bd655737df23da0c2
