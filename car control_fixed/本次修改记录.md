# 小车控制程序本次修改记录

## 2026-09-09：停车时序、运动简报与超长指令处理

### 当前阶段与修改目标

当前基线为 `MECANUM UNIVERSAL V6.3 COMM`。根据本次接续聊天中用户的最新实车确认，W/S、A/D、Q/E/Z/C、R/F 十种运动模式均已稳定，代码与另一控制端已经同步。后续重点是制动终点补偿、距离与角度标定、雷达闭环联调。

本次保留已验证的底层控制，仅修正影响停车时序的日志输出，以及超长协议行的恢复行为。尚未加入制动提前量或 MM/DEG 换算。

原 README 中部分方向“待验证”的状态早于最新实车确认。后续理解当前状态时，以以上确认及新的实验记录为准。

### 文件与备份

- 修改文件：`control.ino`。
- 新增文件：本记录。
- 修改前备份：`backup/20260909_时序与接收修正前/`，包含 `control.ino`、`utils.h`、`utils.cpp`。
- `utils.h`、`utils.cpp`、已有 README 均未修改。
- 主程序保留 V6.3 COMM 标识，在文件顶部注明本次修订日期及记录位置。

### 1. 先制动并读取停车结果，再发送日志

原流程在记录 BRAKE 后，发送 `STOP_BEGIN` 并调用 `Serial.flush()` 等待发送完毕，然后才施加制动。等待期间电机维持原驱动，因此旧数据的 `ENC - BRAKE` 包含串口等待期间的继续行驶量。

修改后的流程：

1. 读取当前编码器位置和动作进度。
2. 保存 `brakeProgress`、`brakeR` 等停车记录。
3. 立即调用原有 `brakeAll()`。
4. 保持原有 300 ms 制动时间，然后释放电机。
5. 读取最终编码器结果，记录 `finalProgress` 和运行时间。
6. 按原顺序输出 `STOP_BEGIN`、`STOP_RELEASED`、详细报告，协议动作最后输出 `@DONE`。

新增的当前位置读取同时改善急停和超时路径：BRAKE 使用停车入口处的新采样，不再直接沿用上一控制周期的位置。速度和控制修正诊断值仍取自最近一次控制更新。

`STOP_BEGIN` 和 `STOP_RELEASED` 现在是停车后补发的事件文本，接收端时间戳不代表物理事件发生时刻。机器协议不依赖这两行。BRAKE 为下达四轮制动前的编码器采样，四轮输出仍按原有 I²C 顺序设置。

`brakeAll()` 本身未修改：电机两端设高电平制动，没有施加反向驱动。

### 2. 运动中使用整行可容纳才发送的简报

原运动状态行较长，在 9600 波特率、UNO 默认发送缓冲区下，会因缓冲区写满而等待，推迟下一次控制更新。原速度计算按控制周期增量工作，这种额外间隔也会影响速度反馈。

本次只修改 `printTelemetry()`，使用以下简报：

```text
M=D P=820/1400 PWM=-128,130,-127,129
```

- 保留模式、动作进度/目标、四轮有符号 PWM。
- 每秒自动简报及运动中按 P 均使用此格式；空闲时 P 的输出不变。
- 使用 64 字节局部缓冲区生成整行，检查格式化结果长度。
- 通过 `Serial.availableForWrite()` 确认整行能放下才写入；空间不足则跳过本次简报，不等待、不排队重试。
- 换行仍为 CRLF，进度显示取整数部分；最终报告和 DONE 的小数精度不变。
- 运行简报不再逐行输出 X/Y/R、Vr、RM、Cr、四轮 V/T、LIM；停车后的原有详细报告完整保留。

此方式不增加常驻日志队列，也不引入新的运动控制结构。它消除了该简报等待串口空间的阻塞；串口格式化、I²C 等仍需要执行时间。此次没有改写调度器，也没有承诺所有输入情况下都严格每 20 ms 执行一次。异常命令回复仍沿用原处理流程。

### 3. 超长协议行丢弃至换行

新增 `protocolDiscarding` 状态。接收超长行时仍回复一次 `@ERR,LINE_TOO_LONG`，随后丢弃该行剩余内容，直到 CR 或 LF 才恢复接收。

丢弃期间包括 W、D、R、X、@ 在内的字符均属于错误行的剩余内容，不会触发单键动作或新的协议解析。CRLF 后的 LF 按原单键空白处理忽略，下一条完整命令可正常解析。

该修正避免了错误行尾部意外启动默认运动。正常协议和正常单键命令的行为保持不变。

### 与另一控制端的兼容性

以下内容保持原样，无需调整另一端现有解析：

- 波特率 9600，命令 `@MOVE,<MODE>,<VALUE>,CNT`。
- 支持的十种运动字符、CNT 定义、坐标与编码器符号。
- 先完整发送 ACK，再启动运动。
- `@ACK`、`@DONE`、`@ERR` 的格式和字段。
- DONE 在详细停车报告之后输出。
- REQ 为请求目标，BRAKE 为制动前进度，ENC 为制动释放后读取的动作进度。
- 人工移动默认目标：平移 1400 CNT、斜移 1200 CNT、旋转 500 CNT。

对外可同步的简要说明：本次为底盘内部时序与错误行恢复修订，机器协议保持兼容；运动中人工简报精简，停车报告保留。新版制动数据应重新采样。

### 验证结果

使用项目现有 Arduino 1.8.15 工具链，以 `arduino:avr:uno` 配置编译通过；未上传硬件。

| 项目 | 修改前 | 修改后 |
| --- | ---: | ---: |
| 程序空间 | 22642 / 32256 字节 | 23790 / 32256 字节 |
| 全局变量 SRAM | 906 / 2048 字节 | 937 / 2048 字节 |
| 扣除全局变量后的 SRAM | 1142 字节 | 1111 字节 |

以上 SRAM 余量尚未扣除运行时栈；简报的 64 字节数组为局部变量。

源码比对确认：运动配置、控制更新、运动学、速度规划、航向修正、轮间 PI、PWM 限幅、电机输出、制动和释放实现、正常协议解析、DONE 与详细报告、启动流程、主循环、单键分派均与备份一致。`utils.h`、`utils.cpp` 也与备份逐字节一致。

停车路径已核对为制动及最终位置采样先于日志发送。按有符号 32 位进度、最大正目标和四轮 -180 PWM 计算，简报为 54 字节，小于 UNO 默认发送环形缓冲区的 63 字节可用上限；代码仍检查实际可用空间。

超长行处理已进行源码路径检查：越界报错后进入丢弃状态，遇换行退出，其间不会进入单键分派。上述检查不代替实车或串口联调测试。

### 下一次实车验证与记录

1. 用原有单键和一条正常 MOVE 命令确认动作、ACK、DONE 正常；运动中按 P 确认简报格式。
2. 先选已熟悉的运动方向，跨过一次每秒简报时刻运行，观察运动连续性及停车结果。
3. 分方向记录 MODE、REQ、BRAKE、ENC、实际位移或角度；制动后增量用 `ENC - BRAKE`，最终目标误差用 `ENC - REQ`。
4. 在抬轮条件下验证超长行尾部含运动字符时不会启动，并确认换行后的正常命令能执行。

停车时序已改变，历史超调仅作参考，不直接写成新版补偿常数。下一步依据新版数据决定提前量，保留现有核心控制参数。

后续每次修改在本文件追加日期、依据、改动范围、验证结果与新的实测结论。

## 2026-09-09：制动补偿主线重新核查

回读原聊天中的用户实测记录，并与当前停车流程、速度规划和编码器采样方式逐项核对。本轮仅新增分析资料，未继续改动源码。

- `标定/历史制动测试数据摘录.md`：保存用户历史消息摘录及 D/Q/R/F 共 12 次正常停车的数值分解，保留原记录缺字。
- `标定/制动补偿分析与测试安排.md`：记录误差分解、提前量与减速区的耦合、最终采样定义，以及六次代表动作的首轮安排。

主要结论：固定提前量仍是候选简化方案，需根据新版最终误差 F−T 验证；F−B 只覆盖误差的一部分。原有 SLOW 区随请求长度缩放，不能假定所有长度都以相同低速进入制动。先收集新版 D=1400、Q=1200 各三次，再决定是否加入补偿及短动作验证方式。真实距离/角度可与最终 ENC 同时测量。


## 2026-09-09：优先非旋转移动测试

根据用户反馈，另一控制端可能不需要旋转，当前优先测试平移。首轮为 D=1400 CNT 三次、Q=1200 CNT 三次；每次等待动作及完整报告结束后再发下一条。后续优先验证实际使用的平移方向和短动作，旋转暂时搁置。此安排仅调整测试顺序，源码与协议均未修改。


## 2026-09-09：新版六次平移基线与首轮提前量试验

用户完成 D=1400、Q=1200 各三次，反馈仅第一次右移略歪，其余无明显问题。D 平均最终误差 +99.58 CNT，Q 为 +101.33 CNT。逐次数据、缺失字段说明、判断和下一轮安排见 `标定/20260909_新版平移基线与首轮补偿.md`。

修改前已备份至 `backup/20260909_首轮提前量试验前_152358/`。本轮新增 currentBrakeLead、brakeTriggerCounts，以及 D=1400、Q=1200 各 100 CNT 的独立试验常量；在动作开始时选择，正常到点判定使用制动阈值。未标定的方向和长度仍提前零计数。请求目标、速度规划、控制参数、原制动流程和机器协议保持不变。人工停车报告附加 TRIGGER、LEAD、STOP_ERROR。

UNO 编译通过，程序 24060 字节，全局 SRAM 945 字节。源码比对确认核心运动控制、速度规划、机器协议和停车执行未变；仅终点比较使用新阈值。尚未上传或完成补偿后的实车验证。下一轮仍为逐次 D 三次、Q 三次，验证最终误差与重复性后再扩大标定范围。


## 2026-09-09：提前 100 CNT 的实车验证完成

用户完成 D=1400、Q=1200 各三次补偿后测试，六次均 TARGET。D 最终残差为 +25.25、+0.25、+19.50，平均 +15.00 CNT；Q 为 +1.00、+18.50、+15.50，平均 +11.67 CNT。平均偏差较无补偿明显改善，逐次波动仍存在，部分直接来自周期检查时越过阈值。数据与分析见 `标定/20260909_提前100验证结果.md`。

本轮不继续调整 100 CNT，也未修改代码。下一轮使用当前版本，逐次发完整协议 D=700、Q=600 各三次，预期 LEAD=0，采集半长度基线；再根据长度证据形成后续补偿规则。当前结论仅覆盖已测代表长度，其他方向仍需后续验证。


## 2026-09-09：暂停刹车标定，优先连续重启排查

用户报告反复 READY，部分启动输出中途再次启动；替换电池电压不足，暂用原电池。本轮回读历史重启讨论、核对当前启动与复位代码、本地 Optiboot 源码及官方电源/复位资料。检查依据、待确认接线和最少排查路径记入同目录《重启排查记录.md》。未修改控制源码；当前首要任务是确认主控供电与 RESET 路径，暂缓下一轮半长度标定。


已根据用户指向，查看指导书接线图并回读更早 V5 回退、架空及疑似上电重启记录。按指导书接线降低 DTR 自动复位优先级；当前重点测电池端、驱动板入口和 UNO 5V，区分电池、连接与逻辑稳压路径。详情追加至《重启排查记录.md》，未改动代码。


## 2026-09-09：串口截图确认误触发路径

截图显示第二次 E 动作为 SOURCE=MANUAL、默认1200；前一 D=700 协议动作已完成。识别出丢失协议前缀时 MOVE 中的 E 可能进入单键入口。截图还显示助手为8N2，代码默认8N1；先对齐停止位并验证 ACK 与 SOURCE。详情记入《重启排查记录.md》。本轮未修改源码。

## 2026-09-09：修复残缺协议误触发手动 E

用户将停止位改为 1 后仍误走 E，说明对齐该设置未解决故障。截图明确 SOURCE=MANUAL，当前先修复接收边界，再通过原始接收字节定位通信问题。

修改前备份：`backup/20260909_接收整帧校验前_164521/`。

本次修改 control.ino：

- 所有普通输入先缓存到 CR/LF；多字符输入统一交给协议解析，解析失败不会逐字符回退到手动动作。因此 MOVE,D,700,CNT 或 E,D,700,CNT 等残缺行被拒绝。
- 保留单键控制：带换行立即确认；不带换行的独立有效单键等待 100 ms 接收静默后确认，控制循环不阻塞。空缓存收到 X 仍立即急停。
- 超长输入丢弃至行尾；含 NUL、非 ASCII 或其他控制字节的行拒绝执行。
- 空闲时输出 RX LEN 与 HEX，记录本次解析前收到的字节；运动中不输出这项诊断。机器 @MOVE/@ACK/@DONE 格式保持原样。
- 显式使用 9600、8N1；启动新增 RX_FRAME_GUARD=1 SERIAL=9600,8N1，便于确认已上传新版本。

未调整运动控制、制动提前量或停车流程。单键与机器协议共享串口，若损坏后的全部输入恰好只剩一个合法单键，仍无法与手动操作区分；本次验证覆盖截图所指向的残缺多字符指令误触发路径。

验证：`tools/car_remote/verify_control_rx.py` 提取实际固件接收函数，以本机 C++ 编译运行；通过正常指令、缺前缀、单键等待、超长后恢复、异常字节、BUSY、X 与分段输入测试。测试替代了串口和电机接口，不代表实车通信链路已验证。UNO 编译通过：程序 24442 字节，全局 SRAM 949 字节，剩余 1099 字节。未上传。

实车下一步：上传 control/control.ino，确认新启动标记；抬轮后逐次发送 @MOVE,D,700,CNT（带换行），收集 RX 到 ACK/ERR 及 SOURCE。正常 RX 应为 LEN=15、HEX=40 4D 4F 56 45 2C 44 2C 37 30 30 2C 43 4E 54。先确认串口执行方向正确，再恢复半长度刹车测试。

## 2026-09-09：补收旧接收版本半长度数据

用户确认未用接收修复版，前三次发送D，随后持续发送同一Q指令。可见执行顺序D/D/D/Q/Q/E/Q/C/Q，E和C均SOURCE=MANUAL、TARGET=1200，另有LINE_TOO_LONG。原始输出及逐次分析已保存至标定目录《20260909_旧接收版本半长度测试分析.md》。D700三次、Q600四次正确模式动作保留为参考，误执行不混入Q标定。未调整代码或提前量；下一步仍上传接收修复并观察RX，通信异常实际来源待确认。

## 2026-09-09：整帧保护现场拦截残缺指令

新日志可见9次协议动作（D三次、Q六次），未见E/C手动动作；另有一条RX明确为OVE,D,700,CNT，缺失@M，程序返回BAD_CMD且未启动。整帧拒绝已获一次现场验证，丢字节来源仍待定位，回传日志亦有截断。原始输出与分析保存于标定/20260909_整帧保护首轮验证分析.md。本轮是否架空待确认，未改补偿参数或代码。

## 2026-09-09：确认落地，通信排查延期，推进全平移联合标定

用户确认整帧保护首轮数据来自落地测试。D700平均残差71.25、Q600为74.25 CNT，因此本轮短档均先试75；D1400/Q1200保留原100。D700～1400与Q600～1200之间线性插值（中点88，整数四舍五入），范围外以及其他方向仍为0。短档与插值需本轮验证。只修改提前量选择与启动版本标记BRAKE_CAL=2，运动控制、减速区、停车流程、接收保护、机器协议不变。

修改前备份：backup/20260909_短距离补偿与区间插值前_170216/。UNO编译通过，程序24574字节，SRAM949字节，剩余1099字节；实际源码接收回归测试通过。尚未上传。

用户明确全部8个非旋转方向均需要；通信缺字和回传截断先记录延期，不再阻塞本轮刹车/物理标定。统一测试单见标定/20260909_全平移24次联合测试.md，D/Q各短中长3次，W/S/A/E/Z/C各短长长3次，总24次；每次同时量实际净位移。完成后集中估计8方向补偿与物理比例，再验证新增补偿。未经实测的方向不直接沿用D/Q参数。

## 2026-09-09：先完成刹车，物理标定另跑

用户明确物理标定单独进行。已读取并保存24次全方向日志；实际LEAD证明板上仍为旧两点补偿。本轮按实际提前量分析，扩展为8方向区间补偿，参数与依据见标定/20260909_八方向刹车补偿第3版.md。备份位于backup/20260909_八方向补偿前_171325/。新增calibratedBrakeLead，启动标记BRAKE_CAL=3 ALL8 SHORT-LONG，核心运动与通信协议不改。

UNO编译24890字节/SRAM949字节，接收和8方向参数边界测试通过。下一轮16次（每方向短长各1）只验证刹车，不量距离；是否收尾根据补偿实测残差决定。物理比例尚未标定。

## 2026-09-09：第3版16次补偿验证完成，补测两个异常点

固件与LEAD确认正确。14次残差在-23.25～+25.50 CNT；Q1200误差-42.50、C600误差-27，两者制动前R已持续偏离，非主动轮计数分别171、-129。详细表与原始证据见标定/20260909_第3版16次补偿验证分析.md。不追单次残差改参数；仅Q1200/C600各3次补测，其他方向不重复，仍先刹车后物理标定。本轮源码未改。

## 2026-09-09：Q/C补测确认提前量偏大，仅修正两个端点

Q1200三次平均误差-36.33，偏航小的一次仍为-31；C600平均-14.33，两个无非主动轮运动样本均值-14.25。修改Q长档90→55、C短档60→45，启动BRAKE_CAL=4。详细依据和原始输出见标定/20260909_QC补测与第4版修正.md；备份backup/20260909_QC补测修正前_172600/。

UNO编译与实际源码参数/接收测试通过，程序24890、SRAM949字节。未上传，下一轮仅Q1200/C600各2次，不量距离，不重跑其他方向。新参数实车验证仍待完成。

## 第4版待验证：新收到四次仍为旧参数

Q1200误差-41.50/-21.50，LEAD90；C600误差+8/-2，LEAD60。记录至QC补测与第4版修正.md。本地第4版参数未再调整，先确认上传及实际LEAD，再进行4次验证，不能将此次作为第4版效果。

## 通信修复候选与标定登记

按用户明确要求冻结第4版刹车参数（Q1200本轮+3/+24，C600+36.5/+2.5），不再追小误差。通信版源码新增带8位标记PING握手、LOG开关，默认省略协议停车长报告及RX回显，保持原启动计时/起步输出、控制与制动时序。备份backup/20260909_通信握手与精简报告前_173548/。配套主线指令发送器已构建，可先握手再单次MOVE、保存日志；不重发不确定移动。

接收源码测试和发送器回复格式测试通过。UNO整机编译的自动审批因额度用尽被拒绝，未能完成；候选固件尚未上传或实车验证。说明位于主线指令发送器/使用说明.md，后续先在IDE编译。

用户已完成独立标定，原始数据写在方案MD，日志为标定输出记录.txt。现场直向长档7000、斜向6000以降低人工相对读数误差，16次均TARGET。已备份原始填写文件，补齐16项ENC并形成标定/20260909_现场长距离标定结果.md。D长档按用户无明显方向问题的反馈统一y=-955。物理系数暂留MD，未改已同步的CNT协议或写入固件。长档LEAD0，不据此自动外推补偿范围。

## 通信修复版整机编译重试通过

按用户“重试”要求重新执行Arduino UNO整机编译，成功。程序25272字节（78%），全局SRAM970字节（47%），剩余1078字节。本次未改源码、未连接或上传小车。已更新主线指令发送器使用说明的验证状态；接下来上传后确认COMM_GUARD=2，先做PING无运动握手，再验证单次MOVE与完整DONE。

## COMM3：按半双工链路修正收发时序，并完成交接文档

核对指导书11～13页确认示例AS69-T20及MD0/MD1=0半双工接法，AUX未连接；回查厂家手册和本地HardwareSerial源码。半双工换向/连续回传是当前待验证解释，不宣称根因已测定。依据保存于通信问题分析与修复状态.md。

本轮备份backup/20260909_半双工时序修正前_214813/。固件仅idle整行收完后增加15ms答复间隔，版本COMM_GUARD=3；实际源码比对确认刹车参数、运动控制、起步/停车、DONE格式不变。UNO编译25292字节/SRAM970字节，剩余1078；接收时序测试通过。

发送器改为持续接收、真正RX静默后发送，新增CHECK20统计首发/重试成功，默认不填充前导、PAD32仅对照。事务测试覆盖分段/丢ACK/丢DONE/错误ACK/异常终止/旧PONG/BUSY/部分写入异常/持续回传/只读批测，确认没有移动自动重发。原始RX字节保存.bin。

已更新主线指令发送器说明，新增项目根目录主线联调交接说明.md，集中记录协议兼容性、8方向距离系数、刹车有效范围、通信状态。未向另一端实际发送。当前软件验证通过，未连接现场串口、未上传、未进行实车通信确认。

## 标定正式接入主程序：MM入口与可调配置

按用户要求，不停留于MD系数表。新增control/calibration.h，8方向CNT/mm小数可独立修改；雷达装车后更新配置并重新编译上传。新增正整数MM指令，CNT原路径兼容，内部毫米换算到整数CNT再走原刹车函数。新增DIST_CAL=1标记。

MM ACK回显MM请求；DONE保留REQ/UNIT=MM并附TARGET_CNT，BRAKE/ENC等仍为CNT。CNT DONE字段保持原样。显式校验正整数/溢出，旋转MM拒绝；整数定点系数与64位中间乘积避免换算溢出。当前不加入EEPROM或串口调参。

发送器同步支持D 100 MM及完整MM命令，并核对MM终态。UNO编译25938字节（80%）/SRAM1055字节/剩余993；实际源码8方向MM转换、元数据、CNT兼容和异常输入测试、发送器MM及CNT事务测试通过。未实车执行新增MM或COMM3。备份backup/20260909_MM接入前_221508/。

主线联调交接说明与标定结果、发送器说明已同步为“已接入”状态。刹车参数仍冻结BRAKE_CAL=4，区间外提前量仍0。

交接包已生成：交接输出/小车主线_标定已接入_20260909_222018.zip。含唯一主线四个源码文件、独立标定配置、统一交接说明、原始标定数据、发送器与测试源码；已核对打包control.ino与编译工作区一致。发送给另一边可交由Codex合并，标定已接入但新MM入口/COMM3仍待现场验证。
