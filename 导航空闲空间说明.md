# 实车开口方向的 1 米空闲空间策略

实车与模拟共用 `NavigationEngine` 和 `MappingRuntime`。模拟使用显式无回波射线清空量程内空间，默认量程为 3 米；实车有效无回波的量程为 1 米。

实车策略：一圈通过采集检查的扫描进入建图后，如果有至少 12 个有效点，对未扫到墙的方向补充最多 1 米的空闲射线。这是用户指定的可通行假设，不是额外测量出的净空。地图仍需累积空闲证据，墙面确认、定位及运动检查继续生效。

原始回波（包括未通过墙面拟合的回波）限制补充射线长度，并按扫描间距、栅格宽度和角度误差保留遮挡区域；已有障碍格也会阻断空闲射线。1 米外保持未知。空扫描不触发补充。默认模拟模式不启用这项假设；实物策略评估通过统一配置启用。棕色地图格标识假设来源，实际自由射线覆盖后替换为测量来源。

模拟程序仍有一个适用范围限制：没有足够的墙面回波，或回波未覆盖至少三个定位扇区时，会停车等待定位。因此，前方是开口但两侧/后方有墙的场景能导航；完全空旷、没有定位结构的场景不保证能启动。本次未放宽定位条件。

局部假墙可在停止采集和运动后通过“局部修正”清除为未知，再由新扫描重新建图。补空闲假设不用于证明终点来路开放，也不能证明漏测方向真实无障碍。
