# 导航响应与墙面快速建图修改说明

已修改根目录正式程序。入口仍为 `start_hardware_navigation.bat`；关闭旧程序后重新启动生效，无需重新烧录。本次未连接串口、未驱动车辆。

## 修改内容

| 项目 | 原因与修改后的行为 |
| --- | --- |
| 点云可见却提示等待 | 显示点云、正式扫描和导航结果原本是不同状态；普通采集进度还会覆盖导航状态。现在采集进度不再把已有的导航判断覆盖成“等待有效扫描”。真正的数据异常仍会显示。 |
| 两圈已确认，墙却迟迟不显示 | 自动导航分支遗漏了两圈证据中的前一圈。现在第一次确认相邻两圈墙面时，将两圈独立证据一起写入地图；后续只增加新一圈，不重复累加旧证据。回归场景验证两圈后墙线可见。 |
| 墙段识别太苛刻 | 按 55 cm 板材的现场条件，最短墙段设为 20 cm，整段拟合残差 RMS 容限设为 5 cm。单个点可以偏得更大，判断整段总体线性；分割拐角时要求两侧都能保留至少 20 cm 墙段，避免距离抖动把长墙切碎。保留连续点和实际间隙判断，不把孤立点或门洞直接连成墙。 |
| 采集端隐藏的严格阈值 | 修复 `scan_geometry.py` 将外部容限硬压回 1.2 cm 的限制；允许配置到 5 cm，并随点密度扩大判断窗口以覆盖 20 cm 实际长度。默认未配置调用仍用 1.2 cm。相邻两圈墙面重复支持允许最多 10 cm 的偏差，以容纳两圈各自正负 5 cm 的拟合偏差。 |
| 正式扫描点数门槛 | 实物导航配置由 40 点降到 24 点，采集端同步采用 5 cm 残差、20 cm 墙段长度。真实零位、时间有效性和角度覆盖/直线结构判断仍生效。 |
| 重复扫描迟迟不行动 | 实物初始化门槛改为两次地图更新。停车后获得两圈可用扫描，墙面或全局定位仍不能确认时，使用当前完整扫描建立临时局部可见空间，尝试约 10 cm 的移动。空间不足时按 2 cm 递减并受底盘范围限制。 |
| 空隙被提前排除 | 没有回波的方向不再直接当作没有候选动作；是否允许移动仍由地图、最新回波和完整车体扫掠检查决定。沿用现有 1 m 范围的空闲假设，并保留障碍后的遮挡。 |
| 小角度绕行 | 同时评估左/右 15°、20°、25°，要求旋转空间和转向后的前进段均可通过，再按前进段障碍余量选择方向。执行转向→重扫→约 10 cm 前进→重扫→回正，各段重新检查。回正误差小于 10°时不继续追逐微小角度。 |
| 旋转指令换算 | 读取 `control/参数工具/旋转停车补偿.json` 的两点角度标定和提前量。20°终点目标为 500 CNT、下发 355 CNT，协议按下发值核对；最终角度先验按实际编码器值反算，补偿只扣一次。自动角度请求限制为 10–25°。 |
| 规划耗时 | 能直行时跳过全图前沿搜索；仅允许向前的模式不再为每个候选重复计算用不到的全图路线进度。相同离线空闲场景、正式地图尺寸下，5 次规划均值由约 201 ms 降到 13 ms。该数据只代表规划函数，不包含雷达转圈、串口握手和实车执行时间。 |

## 两圈等待的含义

这里的两圈是停车后可用于判断的完整雷达圈，不是让底盘原地转两圈。转动周期估计在停车后继续复用，正常情况下首个完整停车圈即可交给处理；最迟在第二圈可用扫描后尝试局部方案。仍需等停车稳定、真实零位和串口完成反馈，不能保证每次从停车到动作都严格小于 3 秒。

局部探索不会把失败的全局定位当作成功，也不会把这次不确定姿态的扫描写入全局地图。无有效新扫描、车体空间不足、底盘执行结果不确定等情况下仍停车。约 10 cm 是目标步长，非强制最低步长。

## 验证

- 新增 `tests/test_navigation_responsiveness.py`，17 项通过：状态覆盖、空隙选择、两圈降级、约 10 cm 步长及缩短、左右绕行与回正、近障碍/未知空间拒绝、旋转补偿与结果反算、墙面两圈可见、过短点簇拒绝、停车后首个完整圈、24 点扫描等。
- 增加 55 cm 墙面叠加约 ±4.5 cm 测距波动、右侧和底部墙面叠加较大单点波动、相邻两圈各偏移 ±4 cm 的用例，均已通过。
- 最终完整回归执行 478 项：468 通过、9 跳过、1 项与本次修改无关的固件文件哈希校验失败。输出见 `tmp/navigation_responsiveness_tests.txt`。
- 失败原因已核对：本地固件文件为 CRLF，清单按 LF 内容计算；将换行归一后两份哈希均与清单一致。本次没有修改固件或哈希清单。
- 门洞、噪声和薄墙相关的 16 项回归通过；`git diff --check` 通过。

完整测试输出保存在 `tmp/navigation_responsiveness_tests.txt`。截图没有原始时间戳与扫描数据，因此验证使用项目既有测试及对应几何场景，尚未确认实车在截图位置的最终行驶效果。
